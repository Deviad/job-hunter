# PRD: LinkedIn research safety without losing research capability

Status: **Implementation in progress. US-001 and US-002 accepted; US-003 state foundation implementation in progress. No deployment.**

Plan of record: this file, following the repository's `tasks/prd-*.md` convention. No code changes, deployment, application submissions, commits, pushes, or pull requests are authorized by this document. Each story starts at attempt 0. Update its checkbox and evidence in the same implementation slice that satisfies it; update this plan before changing scope.

Research date: 2026-09-06. Repository baseline: `a7054d769fd820aa866d040b010b1fea5ef1c401`, branch `issue-7`; working tree was clean before this document was created. That branch belongs to previous work: choose the new issue branch before implementation rather than mixing features.

## 1. Overview

The user needs automated job research that retains LinkedIn discovery and produces CV-grounded matches, compensation comparisons by company/role/location, and UK sponsorship evidence. The immediate problem is a reported LinkedIn account restriction for unusually high-volume **profile data** access. It has not been established which process caused that restriction.

The objective is to eliminate unnecessary access and inconsistent restriction handling while retaining the research workflow. It is **not** to claim that automation can be made undetectable. A smaller request count is an engineering improvement, not proof that LinkedIn permits the access or will not restrict it.

### Agreed product boundaries

- Preserve LinkedIn as a discovery source; do not silently replace it with another board or shrink requested country/role coverage.
- Fetch a needed posting once, retain usable evidence, and do matching/filtering/reporting against saved data.
- Reuse cached content for salary and sponsorship extraction; use employer/ATS and official sources for additional evidence where accessible and permitted.
- Stop on restrictions instead of retrying through them. Continue local analysis and independently available sources, reporting missing LinkedIn coverage explicitly.
- No automated LinkedIn requests during the current restriction, including diagnostics, salary enrichment, refreshes, and fallback helpers.
- No proxy/account rotation, fingerprint spoofing, simulated-human interactions, CAPTCHA bypass, or alternative sessions to route around a restriction. Existing masking is not a safety guarantee and must not become the basis for acceptance.

### Research findings: observations, not incident attribution

Paths below exist in the repository at the baseline. Line numbers are research pointers and will move after editing.

| Observation | Evidence | Consequence for the plan |
|---|---|---|
| Normal orchestration already bounds source/country runs and preflights the exact search URL. | `skills/job-hunter/scripts/jh-search.mjs`, `runCdpPreflight()` around line 197; `skills/job-hunter/references/search-safety-contract.md`. | Extend existing orchestration; do not invent a second search framework. Guard before preflight because the preflight itself loads a page. |
| Search already skips stored jobs with non-empty descriptions and supports explicit refresh IDs. | `skills/linkedin-job-search/scripts/search-linkedin-jobs.mjs`, `loadExistingJobIds()` around line 271 and query filtering around line 1316. | Improve reuse across consumers; do not describe deduplication as a missing feature. A failed DB lookup currently returns an empty set, which can defeat deduplication. |
| JD backfill repeatedly retries blocked pages without a terminating condition in that branch. | `skills/linkedin-job-search/scripts/batch-fetch-jds.mjs`, `processJob()` around lines 660–676: `while (true)`, blocked check, sleep, `continue`. | First mechanical fix: return a terminal block and stop the batch immediately. |
| Search has a finite retry policy but allows multiple blocking outcomes before its origin circuit breaker trips. | `skills/linkedin-job-search/scripts/retry-policy.mjs`, module contract around lines 7–20; `search-linkedin-jobs.mjs`, `scrapeJob()` around lines 862–906. | LinkedIn restriction states need immediate origin-wide stopping, distinct from transient transport failures. |
| A shared lease/budget implementation already exists and explicitly fails open on several errors. | `skills/linkedin-job-search/scripts/cdp-lease.mjs`, module contract and `createSharedBudget()`; `search-linkedin-jobs.mjs`, `enforceRateLimit()` around lines 541–545. | Reuse the component, but add a strict LinkedIn path that cannot silently continue when coordination fails. |
| The LinkedIn salary adapter independently fetches the job URL. | `skills/salary-calculator/scripts/sources/linkedin.mjs`, `fetchExactSalary()` around lines 50–62; `skills/salary-calculator/scripts/lib/enrich-pipeline.mjs` calls it with a saved job row. | Parse saved description content first; salary-only work must not reopen LinkedIn. The adapter comment claiming a request rate respects terms is not evidence of permission. |
| The inline scorer parses the CV as a sanity check but uses hardcoded skill inventories for matching. | `skills/job-match-scorer/scripts/score_jobs_inline.py`, `CV_SKILLS` near line 39, matching near line 389, and `extract_cv_text(cv_path)` near line 568; compare runtime-evidence requirements in `skills/job-match-scorer/SKILL.md`, sections 4–5. | Include a separate, versioned correctness slice. Preserve capabilities, not incorrect historical scores. Every changed result must be explainable by evidence. |
| Installed and repository versions differ. | Byte comparisons differed for `jh-search.mjs`, `search-linkedin-jobs.mjs`, `batch-fetch-jds.mjs`, and the LinkedIn salary adapter. `linkedin-page-state.mjs` matched. Inspected differences include installed country/search configuration support. | Record and reconcile individual differences before deployment; never overwrite the installed tree blindly or bulk-copy it upstream. |
| A real saved-data slice is available without LinkedIn access. | Read-only SQLite query returned 20 LinkedIn rows with descriptions and populated application-link fields. Workspace CV and existing job, score, salary, and metadata tables were present. | Real offline integration is feasible; fake inputs alone are insufficient for acceptance. Populated links have not been validated as working external destinations. |
| No dedicated sponsorship implementation appeared in the scoped source scan. | Sponsorship matches in the inspected skills were guidance, not a sponsor-register importer/resolver. | Add this as a separate enrichment capability; do not claim it already exists. |

Unknown: actual request history, active scheduled jobs, extensions, connected applications, member-profile navigation, browser background/subresource traffic, and LinkedIn's internal detection threshold. Static code findings are plausible contributors, not proof of the restriction's cause.

### External evidence verified during planning

- LinkedIn User Agreement: <https://www.linkedin.com/legal/user-agreement>. The fetched text prohibits scraping/copying service data, unauthorized automated access, and circumventing access/use limits. There is no verified safe requests-per-minute exception. This plan does not establish access permission or guarantee account safety.
- Official UK sponsor register: <https://www.gov.uk/government/publications/register-of-licensed-sponsors-workers>. The page linked a current CSV. A bounded real fetch returned HTTP 206 and parsed these headers: `Organisation Name`, `Town/City`, `County`, `Type & Rating`, `Route`. The inspected slice contained `Skilled Worker` and another route; route filtering is necessary.
- Skilled Worker job guidance: <https://www.gov.uk/skilled-worker-visa/your-job>. It distinguishes an approved employer, a qualifying job offer, and a certificate of sponsorship. A register match alone is not evidence that a particular vacancy offers sponsorship.
- Search-engine results were irrelevant to the requested queries and were not used as evidence. Official pages above were fetched directly without an authenticated LinkedIn session.

## 2. Goals

1. Every unchanged saved-posting analysis pass performs **zero LinkedIn navigations or HTTP fetches** for CV scoring, compensation parsing, sponsorship extraction, sorting, or report generation.
2. After the first observed restriction, no new LinkedIn job/profile/search navigation is admitted by any covered entrypoint. A persisted pause survives process restart. Already-sent requests cannot be retroactively prevented; identify them separately.
3. Preserve the full requested search scope. Every requested query and discovered job has an explicit completed, cached, excluded-with-reason, failed, or deferred outcome; none disappears because a batch/budget limit was reached.
4. Every matched CV criterion, salary, and sponsorship conclusion retains its evidence and uncertainty. Listing-only records are not presented as fully assessed jobs.
5. Keep implementation small: extend existing SQLite storage, search wrapper, classifier, retry policy, lease/budget, salary parser, scorer, and report gate. No new browser, background service, queue broker, or replacement application workflow.

## 3. User Stories

**Proof convention:** existing test paths are identified below. Paths explicitly marked **new** are deliverables to create under existing directories, not claims that those files already exist. Private evidence belongs under `$JOBHUNTER_HOME/runs/<run-id>/research-safety/`, never in published fixtures. Checkboxes below record verified progress; unchecked criteria remain incomplete. There is no existing root lint/typecheck command in `package.json`; do not invent one. Run the relevant tests plus syntax checks for touched modules.

### US-001: Establish the actual runtime and preserve a baseline

**Description:** As the maintainer, I want a source/runtime inventory so that changes address the scripts actually used without losing installed functionality.

**Implementation steps:**
1. Record repository HEAD/status and checksums of the installed/repository entrypoints named in Overview. Inspect exact differences; classify each as runtime configuration, portability/dependency repair, or behavioral drift.
2. Follow imports and process launches from search, backfill, salary, discovery, visible-listing helpers, and application handoff. Inventory every LinkedIn navigation/fetch path, including preflight and direct/debug commands. Do not execute them against LinkedIn.
3. Inspect available local process/scheduler/log metadata and extension manifests read-only. Do not export cookies, tokens, private cache contents, or full profiles. Missing logs mean unknown, not zero traffic. Do not disable anything without separate operational approval.
4. Use SQLite's backup facility to make a private integration copy, including committed WAL data. Select a deterministic saved slice by source/job ID. Record selected identities privately, description hashes, current scores, and salary provenance. Do not change the canonical DB.
5. Append the finalized caller inventory and reconciliation decisions to this issue before editing affected components. If another navigation path is discovered later, update the inventory and its test coverage first.

**Acceptance Criteria:**
- [x] Private `runtime-inventory.json` exists with per-file hashes, caller relationships, drift decisions, and evidence references; its summary explicitly distinguishes observed execution from static capability. Proof: `agent-output/linkedin-research-safety/us001-review-final.json` PASS and `us001-test-final.json` GREEN verify the 53-entry inventory and all 17 grounded paired drift decisions; the 18-test baseline guard passes.
- [x] Private `baseline.json` identifies the bounded real saved-job slice and original source/description/score evidence; a read-only query and backup integrity check are recorded in `baseline-check.json`. Proof: the independent tester reproduced all 20 identity/description hashes and original score/salary-provenance arrays from the hash-verified recorded backup with integrity_check=ok; see `agent-output/linkedin-research-safety/us001-test-final.json` and the separate supervisor replay supplement.
- [x] Repository/installed checksums after research equal the before-research checksums; `baseline-check.json` records that no LinkedIn page was requested and no source/installed script was edited. Proof: the independent tester rehashed all 400 runtime-manifest files without drift or missing paths and checked the recorded acquisition safety statements. Historical acquisition conduct is recorded evidence, not claimed as newly observed traffic. The authorized release-tooling correction does not alter the manifested runtime skill sources.

**US-001 complete.** Independent review passed, independent testing returned GREEN, and the fresh-context final audit passed with report SHA-256 `4807cba69c1a4bb8c6cb99a94e9d4ba5ca6b0cb914706777d848f549d4247b98`. Graph `run_f6f498e5-9fcb-439d-acb3-7409082cc2ff` is terminal. Current evidence is in `agent-output/linkedin-research-safety/us001-audit-final.json`; historical acquisition assertions remain distinguished from newly verified facts.

### US-002: Stop the JD backfill loop on the first block

**Description:** As the account owner, I want a detected restriction to terminate backfill rather than cause repeated retries.

**Execution status:** Complete through independent review/testing and the separate evidence-only closure audit. The runtime change is not deployed. Execution history: planning completed after three launches (two silent Qwen attempts, one of them a dispatcher misselection, then the authorized Codex fallback). Implementation attempt 1 on `mtplx/mtplx-flash-next` (`op_e0eb4f11-b9b8-4b45-a4f3-4673b34035b7`) ended without authoring a report or changing either target. The successful dependency probe is the last recorded tool result; the reason execution ended is unknown. Collection rejected the execution-only projection. The target remains tracked and clean, the new test is absent, and official cleanup verifies process/session/UI absence. Diagnostics are preserved under `US-002/implementation-diagnostics/`. The default-route graph is now cancelled. Recovery attempt 2 uses exact Codex run `run_c80613a6-9cc3-4107-962c-4ed6b4fdccdd`, retaining the accepted plan and two-file ownership. Its settled READY revalidation report is `US-002/recovery-plan.json`, SHA-256 `4060ee1cef23a74b8554cefb8911d1600e847e6e796019a8e4819588a37d78f5`; implementation operation `op_84256370-c2bf-4f64-9126-cbbc3f3a4194` settled with an authored DONE report, verified cleanup and zero ownership violations. Its report is `US-002/implementation.json` (SHA-256 `5b857b162d7b5cb8a8379b0cabfd3f8a9695db76b4092621cf7ebf3c822f0dbc`). Host verification passed all 15 targeted tests and 88 ordinary tests, with zero skips/failures, plus page-state, syntax, release and diff checks. The profile gate actively checked 157 sensitive values without matches. Host logs are `US-002/host-*.log`. Both source/test hashes match the report. Independent review `op_33abe16e-6e04-45ed-a98b-140a25874c10` settled PASS; `US-002/review.json` hashes to `ace2ea2beb1bd7aae6ec81fd8df0a785b2e1df632f698298049dd632e8714e3b`. It verified the current source bindings, all required local review commands and the supervisor preparation, while distinguishing saved export/release evidence from fresh checks. Independent testing `op_12fe06fd-1a13-4b05-97dc-a7b6ef98d1bd` settled GREEN; `US-002/test.json` hashes to `c04cb4a01c8b5e196b8258942c2650ba676f73a4e8b68348b163b7ef7b76869b`. It independently passed the 15 targeted and 88 ordinary tests, page-state and syntax checks, verified current bindings, and confirmed the checkpoint's assertion-preserving move. Its report explicitly does not claim fresh full release/public-export or live verification. Final audit `op_8860db69-ec24-4e4b-8d30-33cd4abf15a8` settled FAIL because original private report provenance was inaccessible to the worker; it independently confirmed the code, tests and preparation. `US-002/audit-fail.json` hashes to `d08220db5f874d9da09ea25e6fcac76e5beb8aad5a42750977e57f01953bd67e`. The build remains honestly blocked at audit. This is an evidence-delivery correction, not a product defect: retain the accepted runtime unchanged and do not redispatch a coder. Split closure into an evidence-only operations graph after providing the missing bindings. US-002 acceptance is completed by the separate bounded closure below; the prior blocked build and failed audit are preserved rather than relabeled. No identical local retry or unavailable-provider attempt is permitted. The source target was tracked and clean at HEAD `f52e41936243c02ce093b06a618a25f998e76f4a`; tracked-work restore snapshot `a47cf1ae72b75bb43a92ff148b6969c374a6ebd4` precedes launch. Do not advance any fallback to unavailable providers. Earlier, default-route graph `run_e4cf24a7-2b44-4770-98f3-790675fc33f8` parked at `thinker_plan` after `alibaba/qwen3.8-max` returned no assistant/tool activity and no authored report. Collection also reported an ACPX cancel/close failure. The retained diagnostic is `agent-output/linkedin-research-safety/US-002/planner-failure.json`; it does not establish a fresh quota error or the underlying cause. Global routing was not changed. The user subsequently confirmed Qwen Max's budget is exhausted and only MTPLX local and OpenAI Codex are available. The requested planning fallback parameters were ignored by `op=dispatch`, which reads only the operation's stored model index and accidentally relaunched Qwen. That second attempt also failed without a report; no further Qwen dispatch is permitted. Use the bundled visible Herdr launcher with the authorized next frozen-chain model, then record its real identity and cross-model reason through the public graph API. The local-first implementation attempt is exhausted for this recovery; Codex handles implementation and acceptance without changing global routes. No global route edit or additional extension repair is in scope. One implementation owner may change only `skills/linkedin-job-search/scripts/batch-fetch-jds.mjs` and the new `test/research-backfill-stop.test.mjs`; supervisor owns all shared preparation and issue updates. Review/test/audit are read-only. The role brief is `agent-output/linkedin-research-safety/US-002/BRIEF.md`.

**Evidence-only closure plan (after audit failure):** Restore snapshot `7bf34bada5e3bd9b6dfd42accc3a9aeca72c42eb` precedes this correction. Supervisor will copy the six original US-002 authored reports, their authoritative settlement/cleanup records and matching read-only GraphStore operation/agent snapshots into `US-002/provenance/`, retaining original paths and SHA-256 bindings in a manifest. A small private checker will reuse the extension's existing `validateSettlementIdentity` function, rather than introduce another identity validator. The operations graph will execute that checker, synthesize the evidence and run a fresh semantic audit; it has no runtime ownership. Preserve the failed build/audit as historical, not rewritten success.
- [x] Original report bytes and current repository copies match all manifest hashes; proof: `provenance-check.log` validates all six entries, supervisor source/copy byte comparisons succeeded, and the independent source verifier repeated the check.
- [x] Registered identity, report hash and cleanup gates validate using the existing settlement module; proof: `provenance-check.log` passed with GraphStore-derived expectations; `provenance-mutation.json` records the intended wrong-hash rejection on a disposable copy.
- [x] Fresh evidence-only semantic audit confirms the supplied bindings resolve the exact missing-provenance finding and preserves prior code/test acceptance and limitations. Proof: `op_8e7e44a7-51c1-4b7b-bd4c-d412b4f5cb71` settled PASS, report `US-002/provenance-audit.json`, SHA-256 `ef9edfd314cb572ccac9bc0669df8e6c82164351539bcdf073b51089ba391585`; closure run `run_ccb9b8e9-157d-465e-ba53-a0997fa00883` is terminal. This accepts delivered-snapshot provenance within the authorized scope, not independent external authenticity, historical acquisition or live/installed compatibility. No deployment is claimed.

**Authorized evidence-output correction:** The first closure source check passed, but graph recording rejected its execution metadata: `checkpointPath` referred to the input manifest and `resultsPath` to its private report, outside the declared output root. Source report `US-002/provenance-source.json` (SHA-256 `4559169fd9d060d68258ca225433f09283e0ca22db50000ba6fb9081e4fe8753`) is retained as a rejected operational candidate, not final acceptance. Run `run_ccb9b8e9-157d-465e-ba53-a0997fa00883` paused for a decision. The user explicitly approved fixing these evidence outputs and finishing acceptance. This narrows the evidence worker's read-only rule: it may write real `results.json` and `checkpoint.json` only in its already-owned `US-002/provenance-verification/` directory. Preserve the exact verifier argv, persist its actual stdout, derive checkpoint state from its actual exit/output, then author corrected execution metadata pointing to those files. Runtime, global configuration and the verifier itself stay unchanged. Restore snapshot: `f3b12ea12867dc5d21747a3a17ab6e1279c81532`. Source attempt 2 produced the real output files and an authored report (`provenance-source-final.json`, SHA-256 `74951708bdea3db1756ba039dc791b43dff3a4a69bb143f40fbb129ab8eb382c`), but used disposable AgentFS mount paths. Post-cleanup audit returned `EXECUTION_ARTIFACT` for those vanished paths. Source attempt 3 is a final metadata-only correction using literal project-relative artifact paths, with a literal parse assertion before ending. Another failure stops the recovery; no runtime or verifier change is warranted. Attempt 3 succeeded after cleanup and strict owned-artifact validation: `provenance-source-accepted.json` hashes to `64a7288bf814c37e405ba2c1690b5e4c30a397ee6582b39a1f5956437b6545fe`, and the graph accepted its real checkpoint/results. Synthesis `op_705f725a-6609-4257-890e-750fdebe51b0` settled DONE (`provenance-synthesis.json`, SHA-256 `04a5af8dc1a66349bb73a357b946b13d85d8732a181c43bd1cc2aa970c2951f0`), independently rechecking the six bindings and portable artifacts while retaining historical failures. Main story ledger has 24 structurally audited entries; the two rejected source candidates remain separate history. Final closure audit passed and the closure run is terminal. The main story ledger now audits all 25 entries, preserving historical failures. Fresh post-audit host checks in `US-002/closure-*.log` passed all 88 ordinary tests with no failures/skips, release verification with 157 sensitive values actively checked and no matches, diff validation and the six-binding provenance checker. Runtime/test hashes remain exactly those accepted by implementation, review and testing.

**Accepted implementation plan:** `agent-output/linkedin-research-safety/US-002/plan.json`, SHA-256 `c807cbaad27295a64cc13db60e52fac13f2710198f9a72fe1c806b87975545b5`, is the authored READY report from the settled Codex planner. Guard CLI execution before imports, extract one orchestration path shared by CLI and tests, and obtain state/reason from the existing canonical classifier. A first-restriction latch must be set immediately at classification, before asynchronous target cleanup, and checked at scheduling and post-wait navigation admission. Blocked/unstarted jobs are deferred without updater calls; already admitted successes can finish. Use the real updater with disposable SQLite and loopback fixtures; preserve unrelated pacing, filtering and transport-error behavior. The small preparation diff remains subject to independent review. No implementation or test success is claimed by this plan.

**Accepted reconciliation:** The grounded US-001 source comparison found that repository/installed batch bodies differ only in their Database binding: the repository uses `workspace-dependencies`, while the installed copy imports `better-sqlite3` directly. Retain the repository binding and installed transactional update semantics; replace their shared blocked retry loop without mixing an import port into this slice. The static comparison does not prove installed resolver compatibility; that remains US-011's rehearsal. Evidence: the `batch-fetch-jds.mjs` finding in `US-001-drift/synthesis.json`, SHA-256 `b7e104382f560dc18055a6221c3969f784e0ecadee5c73077056bf04008f6fd1`.

**Implementation steps:**
1. Change only the blocked branch and its propagation in `batch-fetch-jds.mjs` first. Replace blocked-page sleep/retry with a structured terminal result containing page state and reason.
2. Stop scheduling the remaining batch. Preserve already successful descriptions; leave blocked/deferred jobs eligible for a later explicitly authorized run without storing the restriction notice as their JD.
3. Keep the existing page classifier as the authority; do not duplicate restriction regexes in the batch runner. Export a side-effect-free seam if needed for tests, without running the CLI on import.
4. Keep transport timeout handling separate. A blocking page must not be caught and reclassified as an ordinary retryable error.

**Acceptance Criteria:**
- [x] **New** `test/research-backfill-stop.test.mjs`, test `first restriction ends backfill`, runs the actual batch control flow against a local response fixture and observes no second attempt or subsequent job navigation after the block. Proof: fresh host run in `US-002/host-targeted.log`; named test passed.
- [x] The same test file's `restriction never replaces a saved description` asserts the saved JD is unchanged and remaining IDs appear as deferred in the terminal result. Proof: the named test passed with the production updater and real disposable SQLite, including saved raw/language metadata and prior successful results.
- [x] `node skills/linkedin-job-search/scripts/test-page-state.mjs` and `node --check skills/linkedin-job-search/scripts/batch-fetch-jds.mjs` pass; command results are recorded in the story evidence. Proof: both exited 0 in fresh host runs, recorded in `US-002/host-page-state.log` and `US-002/host-syntax.log`. Independent review, testing and evidence-only semantic closure have passed; the historical acquisition and live/installed limitations remain.

### US-003: Persist a LinkedIn pause and enforce it before all access

**Description:** As the account owner, I want one durable pause that all LinkedIn research entrypoints obey, including after a restart.

**Execution status:** Partial. The state/migration/operator foundation and the wrapper/preflight admission slice S01 (revision 2) are accepted. Other collector, direct-fetch and salary entrypoints are not integrated, so full US-003 remains incomplete. Current evidence and execution authority are in section 9; the following execution history is preserved, not current instructions.

**Historical execution status:** In progress; foundation planning accepted after attempt 2; local implementation attempt 1 failed, Codex recovery authorized. MTPLX worker `dg_run-c689_implement_fe4a7e86`, operation `op_fd6ce007-4554-46f0-8409-1b63ac44b473`, ended with only an execution projection (`piReportProjected:true`), not an authored semantic report. AgentFS export also exited 2; the exact rejected path was not retained. Diagnostics and the raw worker stream are in `US-003/implementation-diagnostics/`. No foundation changes reached the repository; existing targets remain clean and new targets absent. Bundled cleanup succeeded and the private attempt is absent. The old build is blocked, not accepted. The main ledger audits 28 entries; planning provenance is delivered under `US-003/provenance/` using the existing settlement validator. Recovery restore point: `ec6a210fa3330c4e64c213e7ba9263c343cc4d92`. Planner `dg_run-c689_thinker_32ee5673` settled READY on Codex (model attempt 1, transient attempt 1), with verified cleanup and zero ownership violations. Its accepted report `US-003/plan.json` hashes to `bd0c03752f82828041d8c02a98d96791634f2600715e0123f024e3fb64c4e180`; the contract is frozen in section 6 before code. The settlement reports one discarded read-only overlay change; its path was not retained, so attribution is unknown. The foundation targets remain unchanged; this is not a zero-discard or implemented-behavior claim. Pre-implementation tracked-work snapshot: `5f8854b02092742f6669d1f05829191cb67d77c5`. Run `run_c689d590-d1f2-4bb8-8596-910eeaa39e32` uses frozen auto routing with the authorized Codex planning fallback registered through the public API; no unavailable-provider launch occurred. Attempt 1 authored READY but failed settlement because the shared model-store target changed during flight. Candidate `US-003/plan-unsettled.json` (SHA-256 `b008524c80ca13b743ae9dd817400f54f46f234a890b69b4ad92b90185b1fc3f`) and `planner-failure.json` are retained, not accepted. Standalone bundled cleanup succeeded and the old private attempt directory is absent; the original provider-link proof remains failed. A bounded observation found the store unchanged, without identifying its writer or guaranteeing future stability. One same-model planning retry follows the graph's provider-link-churn/not-before control; no configuration repair or runtime changes are authorized by this recovery. Continue the sequential safety track after accepted US-002; this is not a whole-wave barrier run, and US-006/US-008 remain outstanding. Restore point: HEAD `f52e41936243c02ce093b06a618a25f998e76f4a`, tracked-work snapshot `bc55b6981810cd18075d8259f13870bbf987b95d`; current PRD and untracked project instructions are copied under `US-003/`. The migration source/test were verified tracked and clean before planning. Global routing and provider configuration remain unchanged. The user explicitly selected **Use Codex for the remaining PRD work** after the local implementation failure. This supersedes local-first routing for this project only: all remaining roles use per-run `openai-codex/gpt-6-astra` locks; global routing/provider stores stay unchanged. Do not dispatch unavailable providers.

**Bounded execution split:** Implement and independently accept the state/migration/operator-CLI foundation first, then integrate network admission and restriction propagation in a separately planned slice. The foundation owns only `skills/job-hunter/scripts/jh-migrate.mjs`, `skills/job-hunter/scripts/test-jh-migrate.mjs`, new `skills/job-hunter/scripts/linkedin-access.mjs`, new `skills/job-hunter/scripts/jh-linkedin-access.mjs`, and new `test/research-access.test.mjs`. Supervisor owns this PRD, project guidance and shared documentation. No collection, salary, installed-skill or canonical-data edits belong to the foundation. This split does not reduce the full-story criteria below: no claim that every entrypoint obeys a pause is permitted before the later integration proof. The planner must freeze API signatures, record validation and error semantics against the actual callers before implementation; keep one implementation owner and preserve the migration handoff order US-003, US-009, US-005.

**Foundation acceptance (not full US-003):**
- [x] `test/research-access.test.mjs` has passing `status and imports do not mutate access state`, `pause persists across operator processes`, `missing or corrupt access state fails closed`, and `resume requires explicit operator reason`, using real disposable SQLite and the actual CLI/API.
- [x] `node skills/job-hunter/scripts/test-jh-migrate.mjs` proves default paused initialization and idempotence without clearing an existing pause, overwriting ready confirmation or replacing corrupt stored state.
- [x] Syntax checks for all touched modules and the ordinary root suite pass; independent review, testing and fresh semantic audit accept only this foundation. The acquisition-only baseline checkpoint remains unchanged and is not rerun against intentionally changed sources.

The role handoff is `agent-output/linkedin-research-safety/US-003/BRIEF.md`. Any extra caller, schema/API decision or required ownership expansion goes into this issue before implementation. Recovery build `run_9b2c21b2-b251-46a9-88e1-7f9fe2196d0e` uses exact Codex. Revalidation `op_f263b84d-91de-45a2-8b0e-09adadc27b3b` settled READY with verified cleanup, no ownership violations and no discarded changes; `US-003/recovery-plan.json` hashes to `600d3ad6047fbf25619ac2c20411a1ce2470f064eef841ca9086396fedfb6abb`. Host comparison proved its proposed slice exactly equals the accepted plan. Implementation attempt 2, operation `op_5e53402b-686e-47aa-9ca0-e881e6ada791`, delivered the five owned files and an authored DONE handoff (`US-003/implementation.json`, SHA-256 `72a533d3faf0b2ada2cb6de72fdf66161b9179222681c1374cf3029c07c5e190`). Settlement verified cleanup, zero ownership violations and zero discarded changes. Host checks passed seven focused tests, migration tests, all five syntax checks and 95 ordinary tests with no failures/skips; diff validation passed. Release verification failed on a synthetic application timestamp in the migration test. The independent gates are pending; live phase state is held by the graph while repository edits are frozen during worker flight. The main ledger previously audited 29 entries. Recovery preserves the accepted five-file plan and normal gates. Freeze supervisor edits to mounted repository files during each worker flight; record phase progress at settlement. A stale host/overlay comparison after concurrent PRD edits is a possible export-failure cause, not verified attribution. Preserve the full bundled wait output before graph collection; do not claim the absent export-path detail was recovered.

**Review findings recorded before correction:** Host `US-003/host-ambiguous-state.log` reproduces a real temporary SQLite table without key uniqueness, with conflicting ready/paused rows: the read returns `allowed:true`. This violates fail-closed handling of incompatible storage; independent review must adjudicate it. Correction stays within the five-file scope: reject ambiguous access rows with sanitized `STORAGE_ERROR`, preserve their bytes on attempted transitions, and add a real regression in `test/research-access.test.mjs`. Host `US-003/host-release.log` shows the release scanner flagging the synthetic migration fixture; preserve its backfill assertion using a clearly synthetic bound timestamp instead of inline dated application SQL, without weakening or excluding source from the release gate. Restore point before review: `cb3a80e161960995415afd0a4f3067aaca90d7b0`; untracked sources have separate private before-copies. Passing existing tests does not close either finding. The first foundation criterion stays unchecked pending the new corrupt-storage regression; the migration criterion awaits post-correction verification. Independent review `US-003/review-1.json` (SHA-256 `2230522a36ea7fe28b56feb68c6b258db1375470f2ffedf337564a71f71f8535`) freshly reproduced both findings and settled FAIL with zero ownership violations/discards and verified cleanup. Attempt 3 is simplified to these corrections only in `linkedin-access.mjs`, `test/research-access.test.mjs` and `test-jh-migrate.mjs`; the migration and operator CLI source files must remain byte-identical. Do not reimplement the foundation or broaden validation beyond ambiguity. Any further failed correction requires stopping and reslicing, not another whole-foundation retry. The main ledger audited 31 entries before correction handoff. Attempt 3 settled DONE with zero violations/discards and verified cleanup; only the three correction files changed. `US-003/correction.json` hashes to `b56f21afd28f0b9352cf34bdf3550a33495f1ad562aff7dafc1576fe5539c9f9`. The bounded two-row read rejects ambiguity; the new real API/CLI regression proves failures preserve both conflicting rows. Fresh supervisor proof is `corrected-access.log` (8 passing tests), `corrected-migrate.log` (PASS), `corrected-suite.log` (96 passing tests, no failures/skips), `corrected-release.log` (exit 0, active privacy check of 157 values/no exact matches) and `corrected-diff.log` (exit 0). The migration and operator CLI source bytes match the supervisor restore point/before-copy. These satisfy the first two foundation criteria; renewed review settled PASS in `US-003/review.json` (SHA-256 `30e4eafbb6fee1b0d05ddeae0006e035b6841e2a2b79bb5fc01f665da965b1a4`) with fresh bounded SQLite/API/CLI, migration, syntax and release-safety checks. Cleanup was verified with no violations/discards; the main ledger audits 33 entries. Independent test attempt 1 settled NOT_OK because the automatic approval gate denied the access command twice before process creation, not because of a reproduced product failure; `US-003/test-1.json` hashes to `3ffbd30358b46c3d89506ad4c286d96455997907dfebaacfa334656176d184bb`. Migration and pure release-safety checks did run and pass. Its blocked report/settlement/cleanup/registration are preserved under `US-003/provenance/test-blocked.*`; the main ledger audits 34 entries. The user then explicitly authorized the dependency-only `node --test test/research-access.test.mjs` command with its exact workspace environment recorded in private `US-003/EVIDENCE-BRIEF.md`, using disposable databases and without canonical DB access, global permission changes, installation or implementation retry. A pre-audit release check caught the supervisor's hardcoded workspace path in this public plan; the exact authorized argv remains private and unchanged while this record is portable. Retry only this independent test gate under that authorization. The blocked build cannot resume through the current public controls: resolve returns `run is not awaiting a recovery decision`, while record rejects the blocked run and requires resolution. Preserve that blocked history; do not change graph storage or the extension. Separate exact-Codex operations verification run `run_30d10812-5233-4d29-860d-67e5eccb7157` is initialized to execute the authorized command, then synthesize and semantically audit the foundation; source operation is `op_e315eeb9-e5bf-428e-aaf3-5a79c7a78bbe`, with current instructions in `US-003/EVIDENCE-BRIEF.md`. This is an evidence-only reslice, not a new implementation attempt. Its command worker may write only `results.json` and `checkpoint.json` under `agent-output/linkedin-research-safety/US-003/test-verification/` plus its assigned report; all runtime files remain read-only. Those are actual execution artifacts with stable project-relative references and full run identity, not input manifests or report substitutes. The separate command worker settled DONE in `US-003/verification-source.json` (SHA-256 `fc9282ada3180ba7a795c473ac26f3f912fade8f9c516c0026c87b591c40f8bf`): all eight access tests passed after an initial sandbox setup failure and unchanged-command escalation. The portable output artifacts passed strict post-cleanup report validation; settlement verified cleanup and no violations/discards. The main ledger audits 35 entries. Seven completed provenance bindings were delivered and passed the existing verifier; the blocked tester's separate snapshots retain its actual blocked status. Source/copy byte comparisons and read-only registration snapshots are separately supervisor-attributed, not independent external authenticity proof. Synthesis settled DONE in `US-003/verification-synthesis.json` (SHA-256 `8b471dbf84f5308dcafc44287392bd98cdf8c672133a7347157b8fe5301a46fa`), with clean settlement and preserved limitations. Eight completed provenance bindings now pass the reused checker; the main ledger audits 36 entries. Fresh `closure-access.log`, `closure-migrate.log`, `closure-suite.log`, `closure-release.log` and `closure-diff.log` all exit 0 after the portable-plan correction. Final semantic audit remains pending; foundation acceptance is incomplete.

**Implementation steps:**
1. Add the source-access state and API described in Technical Considerations using existing `jh_meta`. Add idempotent initialization in `skills/job-hunter/scripts/jh-migrate.mjs`.
2. Add **new** `skills/job-hunter/scripts/linkedin-access.mjs` for reading/transitions and **new** `skills/job-hunter/scripts/jh-linkedin-access.mjs` for explicit operator status/pause/resume actions. CLI status is read-only.
3. Check state before opening a target, navigating, reloading, fetching, or executing an exact-URL preflight. Integrate `jh-search.mjs`, `cdp-preflight.mjs`, `search-linkedin-jobs.mjs`, `batch-fetch-jds.mjs`, and every additional caller documented in US-001. `--skip-preflight` must not skip the access gate.
4. On `blocked`, `rate_limited`, `active_challenge`, or a login-required/manual-intervention state, persist the pause before releasing the active owner; do not automatically reset it after a timer, new country, new query, process restart, or published restriction-expiry time.
5. Restrict automated top-level LinkedIn navigation to the required jobs routes. Reject member/profile/company exploration for this research workflow. Preserve URLs already embedded in a posting without following recruiter/member links. Inventory browser-generated requests separately rather than claiming a top-level allowlist controls all subresources.
6. The known restricted installation must start paused. Resumption is an explicit operator action after manual review that the restriction is cleared; the recorded confirmation does not establish LinkedIn authorization or guarantee future safety.

**Acceptance Criteria:**
- [ ] **New** `test/research-access.test.mjs`, test `pause survives restart and every entrypoint stops before access`, uses real temporary SQLite state and invokes the covered entrypoints in separate processes; the local navigation server receives no requests while paused.
- [ ] Tests `preflight bypass cannot bypass pause`, `missing or corrupt access state fails closed`, and `resume requires explicit operator reason` pass in that file.
- [ ] Test `research navigation rejects member profiles` rejects profile routes and unrecognized destinations before target creation while allowing the local harness's explicitly bound jobs fixture route.
- [ ] `node skills/job-hunter/scripts/test-jh-migrate.mjs` and `node skills/job-hunter/scripts/test-jh-search.mjs` pass, including idempotent migration without clearing a pause.

### US-004: Coordinate LinkedIn activity across processes without failing open

**Description:** As the user, I want separate helpers to share one access owner and budget instead of multiplying activity unintentionally.

**Implementation steps:**
1. Extend `cdp-lease.mjs`; do not create a competing lock/budget implementation. Add an explicit strict mode for LinkedIn with a stable source-level identity shared by search, backfill, preflight, and any remaining fetch path. Keep unrelated callers' behavior unchanged unless separately approved.
2. In strict mode, lease contention, corrupt state, permission errors, or mutex timeout return a structured blocked/deferred result. Remove the LinkedIn caller's catch-and-continue fallback to per-process limits.
3. Hold exclusive ownership across the network phase. A nested wrapper/child must transfer the same verified ownership context rather than acquire a second independent lease. Each new navigation requires a valid owner, an unpaused source, and a reserved slot immediately before sending.
4. A pause detected by the owner stops further admissions before ownership is released. Pending jobs/checkpoints remain durable. Cancel pending waits cleanly; record any requests already in flight.
5. Preserve existing finite transient retry machinery, but set the LinkedIn blocking-state policy to stop immediately. Do not treat CAPTCHA, restriction, login expiry, or ambiguous access failures as transport retries.
6. Report configured limits as operational budgets, not safe LinkedIn thresholds. Do not add fingerprint masking or artificial human-behavior timing as a mitigation. Remove the existing masking calls from the covered collection path without replacing them with another disguise.

**Acceptance Criteria:**
- [x] Extend existing `skills/linkedin-job-search/scripts/test-cdp-lease.mjs`: `strict LinkedIn callers share one owner` launches real competing subprocesses and proves only the owner reaches a local HTTP target. Evidence (2026-09-11, S08a): section 26 races a holder and a contender fixture process against one loopback counting target — exactly one fetch during contention, contender denied `lease_contended` without touching the target, next caller admitted after release; a subcase runs the real `search-linkedin-jobs.mjs --strict-owner` under a seeded live holder and gets exit 2 `source_blocked` before any CDP phase; and a destination-binding subcase proves non-loopback overrides abort startup.
- [ ] Tests `strict budget I/O failure prevents navigation` and `paused owner releases without admitting queued work` pass with real filesystem/SQLite operations. Holds partially: both named proofs pass (sections 27 and 28) on a real filesystem with real subprocesses — a live foreign budget lock denies every reserve as `storage_error` with zero navigations, a corrupt budget window denies acquisition, and a mid-queue persisted pause denies every queued reserve then releases cleanly. Not met: no case drives real SQLite-backed access state end to end; the queue proof uses the injected/stub module boundary (the persisted-state chain itself is proven separately in the repository-root research access suite). A disposable-SQLite fixture is the remaining piece.
- [x] Extend existing `skills/linkedin-job-search/scripts/test-retry-policy.mjs`: `first LinkedIn blocking state terminates origin` and a separate bounded transient-error case pass. Evidence (2026-09-11, S08a): S6 runs one shared source-wide strict policy across simulated search and detail phases (the caller wiring shape) — the first `rate_limited` observation stops both phases with `SOURCE_TERMINATED`; S7 keeps transient retries finite per key with the source never terminated. Suite exit 0.
- [ ] A local end-to-end collector fixture still extracts the same job fields with the revised navigation path; proof is included in **new** `test/research-access.test.mjs`, not a source-text assertion about removed masking code.

### US-005: Reuse postings across discovery, refresh, and enrichment

**Description:** As the user, I want fresh discovery without repeatedly fetching descriptions already sufficient for analysis.

**Implementation steps:**
1. Keep `jobs` as the description store. Add the small `job_fetch_state` table below; do not create another raw-HTML cache.
2. Retain `(source, job_id)` identity and existing `loadExistingJobIds()` behavior. A failed DB read must stop/defer collection rather than return an empty cache and trigger refetches.
3. Deduplicate exact normalized query/source/country/filter combinations within a scan and reuse completed checkpoint entries on resume. A new explicitly requested scan can discover new IDs; do not impose an undocumented daily search-cache expiry or silently suppress new discovery.
4. New IDs and genuinely missing descriptions are fetch candidates. Existing usable descriptions are reused unless explicit refresh IDs or an approved freshness policy requires new evidence. A CV/filter/salary question is not a refresh reason.
5. Persist content hash and successful-fetch timestamp only after valid content is extracted and saved. Generic `jobs.updated_at` is not a fetch timestamp. Legacy rows with non-empty descriptions but unknown fetch dates remain reusable with `fetch time unknown` provenance; never bulk-refetch them merely to populate metadata.
6. Preserve existing rich descriptions and external links on failed refresh. Advance metadata only on success. Save exclusion and failure reasons separately from the JD.
7. When the wrapper's batch/time budget is reached, checkpoint all remaining requested queries/IDs as deferred. Do not truncate the user's requested scope to make the run appear complete.

**Acceptance Criteria:**
- [ ] **New** `test/research-cache.test.mjs`, test `overlapping queries fetch a new posting once`, executes the real planner/collector against local fixtures and real SQLite; one successful new-ID navigation serves every consumer.
- [ ] Tests `CV or filter changes do not refetch`, `explicit refresh alone invalidates a usable JD`, `cache lookup failure fails closed`, and `failed refresh preserves original description` pass in that file.
- [ ] Test `resume retains every requested query and job` verifies disjoint outcome buckets whose union equals the requested/discovered scope, including deferred work.
- [ ] Existing `test-search-linkedin-jobs.mjs`, `test-extraction-and-filter.mjs`, and `test-jh-search.mjs` pass when invoked from their verified skill directories.

### US-006: Make the inline score depend on actual CV evidence

**Description:** As the user, I want matches to reflect my CV rather than a developer-maintained list of skills assumed to be mine.

**Implementation steps:**
1. Change `skills/job-match-scorer/scripts/score_jobs_inline.py` in a separate correctness slice. Use its real extracted CV text and explicit runtime cache values as possession evidence. A static vocabulary may recognize requirement terms; it must not assert that the user has those skills.
2. Represent extracted mandatory requirements separately from preferred criteria. For each matched item retain the exact job requirement and CV/cache evidence reference. Related technology, generic seniority, citizenship, or residence is not substitute evidence for a missing qualification.
3. Keep the shared role taxonomy and the existing `fit_score >= 60` threshold. Do not change role-classifier boundaries as part of this story.
4. Calculate score from the actual criterion records: matched mandatory count divided by total mandatory count, multiplied by 100. Unknown criteria remain unknown/not matched; preferred items do not enter the denominator. If there are no assessable mandatory criteria, report insufficient evidence instead of a fabricated perfect score. Check existing score-table constraints before choosing the unscored representation and record that choice here before coding.
5. Include CV-content, description-content, relevant cache-data, taxonomy, and scoring-version identities in score reuse/invalidation. Parse the real DOCX once per pass; changing the CV invalidates scores, not job-page content.
6. Preserve old observations; produce a separately identified score run. Do not bulk-overwrite historical scores. Explain each baseline difference as a corrected criterion or evidence change.

**Acceptance Criteria:**
- [ ] Extend existing `skills/job-match-scorer/scripts/test-score_jobs_inline.py`: `same job differs when CV evidence differs` uses DOCX inputs parsed by the actual extractor and verifies criterion evidence, not just CV hashes.
- [ ] Tests `catalog term alone is not CV evidence`, `preferred requirement does not reduce fit`, `unknown authorization is not inferred`, and `score equals persisted mandatory counts` pass.
- [ ] The private real saved-data slice produces `cv-score-diff.json` with evidence for every score change. Repeating the same inputs produces the same criterion records and makes zero LinkedIn requests.
- [ ] The existing Python test file passes via its documented/direct invocation, and `python3 -m py_compile skills/job-match-scorer/scripts/score_jobs_inline.py` succeeds.

### US-007: Extract pay from saved content and compare it honestly

**Description:** As the user, I want to see better-paying roles and companies in the selected location without reloading LinkedIn for salary research.

**Implementation steps:**
1. In `sources/linkedin.mjs`, reuse `parseSalaryCandidates()` on the saved posting's richest available description. Keep parser section-boundary protections. Do not call `httpClient.get(job.url)` for LinkedIn salary-only enrichment.
2. Extend `lib/enrich-pipeline.mjs` to carry cached-content provenance and unchanged-content reuse. If saved content has no salary, move to the existing advertiser/ATS scan when a suitable external application URL exists, then the existing benchmark adapters. No LinkedIn refetch fallback.
3. Decode an already-saved LinkedIn external-application redirect locally rather than visiting it. Validate destination scheme/host and each redirect; reject credentials, local/private network targets, and unexpected LinkedIn destinations in automated external research. Treat a challenge as missing data/blocked, never solve or retry through it.
4. Reuse `job_salary_observations`, `salary_benchmarks`, `lib/salary-db.mjs`, `lib/salary-selector.mjs`, and normalization/annualization code. Preserve the posted-versus-estimated partition and historical benchmark linkage.
5. Compare only compatible currency, pay period, compensation type, and location scope. Keep base salary, total compensation, and contract/day rates separate unless an explicit existing conversion basis is shown. Unknown pay sorts last, not as zero.
6. Rank posted salaries separately from market estimates. For a company/role summary, show the median of comparable posted-range midpoints and the sample count; label it `among collected postings`, not a market-wide company-pay claim. A generic role benchmark cannot establish that one named company pays more than another.

**Acceptance Criteria:**
- [ ] **New** `test/research-salary.test.mjs`, test `saved LinkedIn salary needs no network`, feeds a persisted posting through the actual enrichment pipeline and fails on any LinkedIn request.
- [ ] Tests `cached absence uses external or benchmark evidence`, `similar-job salary is excluded`, `external redirect cannot bypass source pause`, and `posted and estimated rankings remain separate` pass.
- [ ] Test `company comparison uses only comparable posted observations` computes expected aggregates from the underlying fixtures, including sample count and unknown-pay handling.
- [ ] The private real slice produces `salary-reuse.json` with selected observation IDs, provenance, and zero LinkedIn fetches; existing salary parser checks and `node skills/salary-calculator/scripts/test-itjobswatch-parse.mjs` pass.

### US-008: Cache and validate the official UK sponsor register

**Description:** As the user, I want employer sponsorship capability grounded in the official register rather than search snippets.

**Implementation steps:**
1. Add **new** `skills/job-hunter/scripts/uk-sponsors.mjs` with separate fetch/parse/normalize/match operations. Fetch the official publication page, select its worker sponsor CSV link, and validate HTTPS plus the expected government asset host. Do not hardcode the dated CSV URL from this research.
2. Stream/download to a temporary workspace file with size/time bounds; verify the required headers, parse quoted CSV correctly, and retain original values. Publish a new snapshot atomically only after successful validation. Never replace the last good snapshot with a partial or malformed download.
3. Keep snapshot bytes and a manifest under `$JOBHUNTER_HOME/sponsor-register/`. Manifest fields are defined below. Refresh once per batch when explicitly requested or its configured cache policy says due, never per job.
4. Build an in-memory lookup once per batch. Match normalized exact legal names first; only owner-confirmed aliases can produce a confirmed alias match. Preserve punctuation/spacing normalization separately from legal-entity identity. Do not collapse unrelated names merely by stripping corporate suffixes.
5. Filter specifically for the relevant `Skilled Worker` route and retain rating/type information. Fuzzy similarity, staffing agencies, parent/subsidiary assumptions, duplicate names, and conflicting town/city evidence produce candidates/ambiguity, not confirmed employer matches.
6. Use an existing CSV dependency only if declared and appropriate; otherwise add one maintained parser and update the dependency/release documentation in the same slice. Do not implement CSV with `split(',')`.

**Acceptance Criteria:**
- [ ] **New** `test/research-sponsorship.test.mjs`, tests `quoted register rows preserve legal identity`, `wrong route is not Skilled Worker evidence`, and `ambiguous employer remains unresolved` pass.
- [ ] Test `failed refresh retains last good snapshot` exercises real temporary files, checksum verification, and atomic replacement.
- [ ] A bounded real official-register rehearsal produces private `sponsor-register-check.json` with source URL, fetched timestamp, required headers, byte/hash evidence, and parsed row count. Synthetic rows alone do not satisfy this criterion.

### US-009: Distinguish an employer licence from sponsorship for the role

**Description:** As the user scanning the UK, I want an explicit answer about sponsorship evidence for each vacancy, including uncertainty.

**Implementation steps:**
1. Add a `job_sponsorship_observations` table using the data contract below. Link observations to a job and the precise register snapshot/source document used.
2. Parse the saved JD for explicit positive, explicit negative, conditional, or absent sponsorship wording. Store the exact quotation and evidence location. Check negation; `cannot sponsor`, `no sponsorship`, and role-specific right-to-work requirements must not become positive signals.
3. Enrich from an employer's role page/policy only when the available external URL is allowed and the evidence applies to the advertised legal employer and location. Do not follow recruiter profiles or treat a staffing agency's licence as its unnamed client's licence.
4. Combine role evidence and employer-register evidence without collapsing them into one boolean. `Licensed employer; this role unknown` is a valid and useful result. `No confirmed register match` is not a legal conclusion that the company cannot sponsor.
5. Apply the user's country-specific sponsorship need from runtime cache. Unknown need stays unknown. Explicit no-sponsorship is a blocker only when sponsorship is required; ambiguous cases remain visible for review rather than being silently removed.

**Acceptance Criteria:**
- [ ] Extend **new** `test/research-sponsorship.test.mjs`: `licensed employer does not imply role sponsorship`, `explicit role refusal overrides company-wide optimism`, `agency licence is not client evidence`, and `unknown need remains unknown` pass.
- [ ] Every resolved outcome in private `sponsorship-slice.json` has a quote or register snapshot/row reference; unresolved outcomes carry a reason and are not rounded up to sponsored.
- [ ] A report fixture includes confirmed route capability, explicit vacancy sponsorship, explicit refusal, and unknown/ambiguous cases as separate outputs, verified by **new** `test/research-report.test.mjs`.

### US-010: Produce a complete research report from saved evidence

**Description:** As the user, I want CV matches, pay comparisons, sponsorship, and source coverage in one useful report even while LinkedIn is paused.

**Implementation steps:**
1. Add **new** `skills/job-hunter/scripts/jh-research-report.mjs` as a read-only renderer over saved results. It is not another search orchestrator. Proposed CLI inputs: `--search-id`, `--db`, `--json`, and optional explicit output path; validate these against existing CLI conventions during implementation.
2. Preserve the existing presentation columns and `Full facts` labels documented by the installed job pipeline. Add UK sponsorship to each job's reason/evidence details and to the machine-readable row, not by silently deleting existing columns.
3. Include source job URL, description freshness/unknown timestamp, score basis, salary provenance, employer-register state, role sponsorship state, and evidence URLs. Put pay comparisons in a separate location-scoped section.
4. Extend `jh-report-gate.mjs` to enforce requested-run coverage and evidence gaps. A paused/missing requested LinkedIn scan yields a partial report, not zero jobs or a complete search. Locally complete analysis of previously saved jobs may still be shown as complete analysis of that explicit saved subset.
5. Report aggregate counters with precise meanings: attempted/admitted top-level navigations, cache reuse, completed queries, deferred queries/jobs, blocking outcome, and already-in-flight requests at pause. Do not call these LinkedIn's total request/account counters.
6. Retain fit-first shortlist behavior; add an explicit pay comparison rather than allowing a high estimated salary to hide a poor match. Do not initiate applications from this renderer.

**Acceptance Criteria:**
- [ ] **New** `test/research-report.test.mjs`, test `paused LinkedIn still yields an honest saved-data report`, verifies useful rows, CV/pay/UK sponsorship fields, unchanged mandatory presentation columns, and explicit partial source coverage.
- [ ] Tests `every requested query has an outcome`, `report aggregates recompute from rows`, and `read-only report cannot navigate or submit` pass using real SQLite and a network-forbidden test boundary.
- [ ] `node skills/job-hunter/scripts/test-jh-report-gate.mjs` and `node skills/job-hunter/scripts/test-jh-freshness.mjs` pass, preserving stale-posting and salary-provenance behavior.

### US-011: Prove the pipeline on real saved data and roll out reversibly

**Description:** As the maintainer, I want evidence that the actual installed dependencies and saved data work, without testing against the restricted account.

**Implementation steps:**
1. Add **new** `skills/job-hunter/scripts/rehearse-research-safety.mjs` with explicit database/CV/output inputs. It backs up to a private working copy, takes a bounded saved-job slice, calls the actual parser/scorer/salary/sponsorship/report components, and refuses LinkedIn network access throughout.
2. Separate semantic fixtures from real-dependency proof. Local HTML/CDP/HTTP fixtures prove block, ordering, argv, file-descriptor, retry, and exit-code behavior. The real SQLite/CV slice proves the actual computation loads and runs. The official-register fetch proves the external parser on real data. Neither proves LinkedIn's current live page compatibility.
3. Save a private verification summary and compare baseline IDs/evidence. Traffic-only slices must preserve outputs. The CV correctness slice and genuinely new salary/sponsorship evidence may change outputs only with per-row explanations.
4. Run the relevant existing skill tests, new root tests, `npm test`, and `npm run verify:release`. Update affected skill instructions, `README.md`, `docs/security-and-privacy.md`, and `search-safety-contract.md` so they agree with the actual stop/cache behavior. Update dependency documentation only if a dependency changes.
5. Before any installation, review the per-file drift decisions from US-001. Use `node scripts/install.mjs --dry-run` and review its manifest. Deploy only with separate authorization and an installed-tree restore point; never install directly from an unresolved drift state.
6. Leave LinkedIn paused after offline verification. A future live canary requires separate explicit authorization and manual confirmation that the account restriction has cleared. It must be bounded, use the real implementation, and stop on the first warning. No repeated canaries to find an enforcement threshold.
7. Rollback restores the code/installed files and private DB backup when needed. Never roll back a persisted restriction into an unpaused state. Do not discard the evidence explaining why access was stopped.

**Acceptance Criteria:**
- [ ] Private `verification.json` references the real slice, CV parser result, score differences, salary provenance, sponsorship evidence, report path, SQLite integrity result, and zero LinkedIn requests. All referenced artifacts exist and their hashes match.
- [ ] `npm test`, `npm run verify:release`, and the relevant existing skill-test commands listed in this plan pass with captured exit codes; unmet checks remain unchecked with the exact blocker.
- [ ] Private `install-preview.json` records the dry-run manifest and accepted drift decisions; no deployment is claimed from a dry-run result.
- [ ] The final handoff explicitly lists offline verification done, live LinkedIn compatibility unverified, account-safety guarantee unavailable, and deployment status. No live-canary criterion is marked passed by a fixture or by the absence of a notice during a short run.

## 4. Functional Requirements

- **FR-1:** The system must retain requested LinkedIn discovery, CV matching, location-scoped compensation research, and UK sponsorship analysis as distinct capabilities.
- **FR-2:** The system must treat a reported/detected restriction as a durable source-level pause, checked before any new covered LinkedIn access, including preflight, reload, direct helper, and fallback paths.
- **FR-3:** The system must not automatically clear a pause based on elapsed time, retry budget, a new process, a new country, a different session, or another account.
- **FR-4:** The system must stop the origin on the first blocking page state and preserve already collected evidence. Transient errors must remain distinguishable from access controls.
- **FR-5:** The system must use strict shared ownership/budget coordination for LinkedIn and must fail closed when coordination or required cache state is unreadable.
- **FR-6:** The system must not navigate member/recruiter profiles as part of job research. Browser-generated subrequests must not be misreported as controlled top-level requests.
- **FR-7:** The system must reuse valid saved descriptions across CV scoring, filtering, pay parsing, and sponsorship extraction. Reanalysis must not imply a job-page refresh.
- **FR-8:** The system must retain all requested query/job outcomes in checkpoints and reports, including deferred items caused by budgets or restrictions.
- **FR-9:** The system must store successful-fetch metadata separately from general row update timestamps and must not invalidate useful legacy content just because that metadata is absent.
- **FR-10:** The system must ground every matched criterion in the user's actual CV or explicit cache evidence and calculate the score from its recorded criterion counts.
- **FR-11:** The system must distinguish posted pay from estimates and compare only compatible compensation observations with sample/provenance disclosure.
- **FR-12:** The system must parse LinkedIn salary evidence from saved content and use allowed external evidence/benchmarks rather than independently refetching LinkedIn.
- **FR-13:** The system must validate and cache the official UK sponsor register, preserving route, legal-employer identity, snapshot age, and ambiguity.
- **FR-14:** The system must represent employer licensing, role-specific sponsorship, and the user's need for sponsorship separately. A licensed employer must not automatically become a sponsored vacancy.
- **FR-15:** The system must retain explicit unknown/conditional/conflicting evidence rather than inventing pay, CV qualifications, or sponsorship availability.
- **FR-16:** The system must provide useful saved-data results while clearly marking uncompleted requested source coverage as partial.
- **FR-17:** The system must keep credentials, the CV, private job text, and runtime evidence local by default; external requests must not include CV or personal-cache contents merely to enrich a posting.
- **FR-18:** The system must keep migrations idempotent, preserve historical salary/score evidence, support backup-based rollback, and preserve restriction state across rollback.
- **FR-19:** The system must reconcile installed/repository drift before deployment and keep touched code, callers, tests, configuration, and documentation consistent.
- **FR-20:** The system must not claim compliance, a safe request threshold, undetectability, or a no-restriction guarantee from this engineering work.

## 5. Non-Goals

- Bypassing LinkedIn restrictions or making prohibited access appear human.
- A guarantee of no future notices, or a claimed diagnosis of which extension/process caused this notice without runtime evidence.
- Expanding recruiter/member-profile collection, messaging, connection requests, or automated application submissions.
- Replacing LinkedIn without the user's agreement, reducing requested locations/roles silently, or treating listing snippets as full job assessments.
- Rewriting the role taxonomy, all ATS helpers, the browser stack, or the entire scoring/application architecture.
- Building a distributed queue, new daemon, new browser, elaborate event platform, or a generic scraping framework.
- Giving immigration/legal advice or certifying visa eligibility. Sponsorship research is evidence-backed screening, not a visa decision.
- Publishing private CV/job/browser fixtures or bulk-refreshing all historical jobs to populate new metadata.

## 6. Design / Technical Considerations

### Implementation order and ownership

Execute US-001 first and freeze the shared access-state, fetch-metadata, CV-evidence, salary-provenance, sponsorship, and report contracts before parallel implementation. The baseline must identify actual callers and installed drift; workers must not invent competing interfaces from their own assumptions.

#### Parallel tracks and completion dependencies

| Track | Work that can proceed independently after US-001 | Completion dependency |
|---|---|---|
| Access safety and caching | US-002, then US-003, then US-004, then US-005. Keep this track sequential because the stories share collection code. | Each story follows the preceding story's verified handoff. |
| CV-grounded scoring | US-006 can run alongside the safety/cache track. | Requires the baseline and agreed evidence contract, not live LinkedIn access. |
| Cached salary enrichment | Isolated US-007 parser/pipeline work can start alongside safety work against the frozen contracts. | Full-story acceptance must verify integration with completed US-003 through US-005. Early isolated work is not a completed US-007. |
| UK sponsorship | US-008 can run independently; US-009 follows the validated register importer. | US-009 also needs an exclusive migration-writing slot and ownership of its report-test additions. |

US-010 joins completed cache, scoring, salary, and sponsorship work. US-011 follows the integrated report. Independent unit-test success does not satisfy these integration dependencies.

#### Conservative schedule for a wave runner

If the runner waits for every story in a wave before advancing, use this whole-story schedule. Stories listed together may execute in parallel; the next wave starts only after every listed story has met its acceptance criteria and its artifacts have been verified.

| Wave | Stories | Reason |
|---|---|---|
| Baseline | US-001 | Establish evidence, caller inventory, contracts, and path ownership. |
| Parallel foundations | US-002, US-006, US-008 | Backfill stopping, CV scoring, and register import have separate implementation paths. |
| Durable access gate | US-003 | Requires the backfill-stop handoff; owns the access-state migration. |
| Coordination and role sponsorship | US-004, US-009 | Coordination follows the access gate; sponsorship follows US-008. US-009 has the migration slot while US-004 owns lease/retry code. |
| Shared posting reuse | US-005 | Requires completed safety gates and the migration handoff from US-009. |
| Salary integration | US-007 | Validates cached salary work against the completed access/cache implementation. |
| Combined report | US-010 | Joins all required research capabilities. |
| Integrated verification | US-011 | Verifies the actual combined implementation and prepares reversible rollout. |

For more concurrency, the supervisor may split US-007's isolated preparation from its integration acceptance into explicit owned tasks in this plan before dispatch. Do not place an unfinished US-007 in an early blocking wave while its prerequisites sit in a later wave; that creates a dependency deadlock.

#### Supervisor execution log (updated during implementation)

Tracker issue: #9 (created 2026-09-06). Multi-agent route: Delegate Graph (pi-agent-wave) over Herdr, one visible worker tab per graph operation. Wave-to-run mapping is recorded in issue #9.

Restore points recorded before any implementation slice:

- Git: branch `issue-9-linkedin-research-safety` created from `origin/main` `cc72ca3` (contains the plan-of-record baseline `a7054d7`). PRD committed as `f52e419`.
- Database: `sqlite3 .backup` copy `~/.job-hunter/backups/jobhunter-pre-research-safety-20260906T062757Z.sqlite`, `PRAGMA integrity_check` = `ok`, 3308 `jobs` rows, SHA-256 `750254e5300ee7804867c255c3d7957e42a44d969725a321b12d9b785d1bd2a7` (recorded in `~/.job-hunter/runs/linkedin-research-safety-20260906/research-safety/baseline-backup.sha256`).

Evidence-location amendment (narrows the "Private evidence" convention in section 3). The pi-agent-wave worker sandbox runs with a temporary `HOME` and an AgentFS overlay rooted at the supervisor's working directory, and only graph-owned paths under that directory are exported. A worker therefore cannot durably write `$JOBHUNTER_HOME/runs/<run-id>/research-safety/`. Private artifacts are written by workers under the gitignored repository path `agent-output/linkedin-research-safety/<US-xxx>/`, and the supervisor copies each wave's artifacts to `$JOBHUNTER_HOME/runs/linkedin-research-safety-20260906/research-safety/<US-xxx>/` at wave close. The public-copy prohibition is unchanged: nothing under `agent-output/` is tracked or installable, and published skill files must keep resolving paths through `$JOBHUNTER_HOME`/`process.env.HOME` so `npm run verify:release` continues to reject machine-absolute paths.

Execution-route findings recorded while starting wave 1 (all verified on this machine, 2026-09-06):

- `z.ai-sub/*` is quota-dead: `pi -p --model z.ai-sub/glm-5.2` returns `429: {"code":"1113","message":"Insufficient balance or no resource package. Please recharge."}` (same for `glm-5.1`), and `claude-code/claude-opus-5` returns `Request error`. Live and verified: `openai-codex/gpt-6-astra`, `openai-codex/gpt-5.6-luna`, `alibaba/qwen3.8-max`, `alibaba/qwen3.8-flash`, `alibaba/glm-5.2`. `~/.pi/agent/model-routing.jsonc` was reordered so every wave role (`reasoning`, `coding`, `review`, `test`) starts on a live model, with the dead providers kept as tail fallbacks. Restore point for the pre-edit state: `git stash create` commit `e7be2e198295d93d0200e08b4729e92b6d8d3dae` in the `~/.pi/agent` repository (file SHA-256 before edit `e0dc8e5b4383c0ecaed7bd9be7ef1cb369b2c13868956535c3797cec053c8540`).
- The installed pi-agent-wave build never advances `model_attempt`: `selectModelFallback()` in `retry.ts` has no production caller, and `op=dispatch` always launches `route.chain[operation.model_attempt]`. A dead chain head therefore blocks a run instead of failing over, so routes must be live before `op=init` (run 1 `run_891a299e-57bc-447f-a416-7ca569f5e2e1` was discarded for exactly this reason; wave 1 runs as `run_7982d82a-59c9-4882-ab6d-cf2726f607fe`).
- Supervisor must inspect every worker report for the execution-only projection. When a Pi-route worker exits 0 without writing its report, `scripts/acpx-worker.ts` `projectPiReport()` writes `{verdict: canonical-positive, claims:["Supervisor projection: ... no semantic task claim is inferred"]}` and the gate accepts it, which would otherwise pass an empty plan as `READY`. A report whose claim statement starts with "Supervisor projection" is treated as an operation failure, not a result.
- Do not run anything that rewrites `~/.pi/agent/auth.json` (headless `pi` invocations, provider token refresh) while a worker attempt is in flight: settlement verifies the attempt-private provider link target and fails with `provider credential target changed`, which wedges the operation because the ACPX session is already closed and `op=cancel` then cannot prove the cancelled transition.
- Supersedes the second bullet above for the build loaded now: `pi-agent-wave-new-design` merged `9178183`/`4fcab1a` (worker-route resilience) at 12:36 local and `store.ts:961` now calls `selectModelFallback(...)`, so the loaded extension is no longer the build where `model_attempt` could never advance. Whether a transient failure advances the frozen chain in production is still **unverified** here: `op=dispatch` launches `--policy auto --model <chain head>` and the launcher reports `tier=exact`, `chain-length=1` for the attempt, so treat same-model exhaustion as a park until an advance is actually observed.

Wave 1 restart (2026-09-06 11:06 UTC, supervisor session `01a0764c`):

- `run_7982d82a-59c9-4882-ab6d-cf2726f607fe` was orphaned, not paused: its supervisor Pi session ended at 06:58Z, which killed the worker Herdr tab `wK:t5` and its ACPX session, while the graph row still recorded `acpx_state=alive`. Neither converging op can settle such a run — `op=collect` throws `ENOENT: no such file or directory, scandir '/private/tmp/delegate-graph-herdr-run-7982d82a-...-op-f7366205-....h3zligu0'` because the attempt directory was already removed, and `op=cancel` throws `failed to cancel ACPX attempt ...:thinker:0:0:openai-codex/gpt-6-astra:codex` because the recorded state is `alive`. Only `op=record status=failed` is accepted, and an `error` string that `classifyFailure()` does not recognize lands as `classifier_reason=unclassified`, which moves the run to `awaiting_user` and then `blocked`; `op=resolve decision=retry` on it then fails with `run is not awaiting a recovery decision`. Operational rule: a run whose worker died with its supervisor is abandoned and re-initialized, and it must be named in a `last_error` shape the classifier recognizes if resume is intended.
- Wave 1 re-initialized as `run_98a66fd0-c464-4e07-b513-38be951ed91d` (story `linkedin-research-safety-us001`, `build`, policy `auto`, digest `452dba1e...`). Thinker op `op_fcf814ad-ac33-4758-b3e6-2ffa102f0cec` dispatched 11:06:28Z over Herdr to agent `dg_run-98a6_thinker_e6870d92`, tab `wK:t6`, pane `wK:p6`, model `openai-codex/gpt-6-astra` (attempt 1/4, verified live by the probe recorded above), report path `/private/tmp/delegate-graph-herdr-run-98a66fd0-...-op-fcf814ad-....e8vfqci0/report-op_fcf814ad-ac33-4758-b3e6-2ffa102f0cec.json`. The old run is left `blocked` for audit; nothing from it is assumed to exist.
- Restore point for repository state at dispatch time: HEAD `f52e419` plus `git stash create` commit `16d93aa4f577e7618a0f23a794ddc111b6947810` (dirty file: this PRD, uncommitted documentation amendment only).

Wave 1 continuation (2026-09-06 11:20-21:00 UTC, supervisor tab `wK:t1`, sessions `01a07667` then `01a0784b` after the extension update):

- Route re-probe: `openai-codex/gpt-6-astra` is quota-dead, not transient-throttled. Worker `wK:t6` stream ended `codexErrorInfo=usageLimitExceeded`, "try again at Sep 7th, 2026 4:26 AM", and `pi -p --model openai-codex/gpt-5.6-luna` returned `Codex error: The usage limit has been reached`, so the whole `openai-codex/*` family shares one exhausted plan. Live-verified again by direct call: `alibaba/qwen3.8-max`, `alibaba/qwen3.8-flash`, `alibaba/glm-5.2`. `~/.pi/agent/model-routing.jsonc` reordered so `reasoning`, `review` and `long-context` head on Alibaba with the dead providers kept as tails; pre-edit restore point is object `af4d692ad4c2f00fc3a9748a352e8ff1851ed412` in the `~/.pi/agent` repository, recorded in `~/.pi/agent/notes/restore-points.md`. A worker route must be probed live before `op=init`, because the chain is frozen into the run at init.
- `run_98a66fd0` converged and is abandoned as planned: `op=collect` recorded `worker-runtime-failure` (retry 1/3 on the dead head), `op=cancel` then threw `failed to cancel ACPX attempt ...` exactly as the rule above predicts (its ACPX session was already closed), and `op=record status=cancelled` converged it: operation `cancelled`, run `cancelled`. A later queued notice claiming "retry 2/3 is due" for that run was stale and was checked against `op=status` before acting, not obeyed.
- `run_aa7fb2d4-571a-4722-924a-b479f4d12550` was initialized with a 329-byte task, then deliberately cancelled ~1 minute in: that task omitted the no-LinkedIn, read-only-`$JOBHUNTER_HOME` and no-headless-`pi` rails, and a safety story must not dispatch a worker whose constraints live only in a file it might skim. `op=cancel` succeeded here while the worker was still alive, which is the difference from the closed-session case above.
- Wave 1 now runs as `run_9b5379bb-95e7-4989-b10b-7636b8a538f8` (story `linkedin-research-safety-us001`, `build`, policy `auto`, digest `f00d4120...`, frozen chains thinker `alibaba/qwen3.8-max` first, implementer `alibaba/qwen3.8-flash` first, reviewer `alibaba/qwen3.8-max` first). Thinker op `op_b5ed07d1-060f-42fa-9504-60ac3f994724`.
- Thinker attempt 1 (agent `dg_run-9b53_thinker_954ddee8`, tab `wK:t8`) made 76 real tool calls and then ended `end_turn` with an empty assistant message and no report; `projectPiReport()` wrote a 632-byte `verdict READY` projection. The merged `9178183` resilience work holds here: `projectedAttemptFailure()` in `op=collect` refused it and recorded the operation failed instead of advancing the graph on an empty plan. The attempt was then re-dispatched as transient retry 1.
- Settlement hazard confirmed on this run: `op=collect` recorded `provider credential target changed: $HOME/.pi/agent/models.json` (the absolute path is under the machine home directory) because the host provider store moved between dispatch and collect. The Pi route reads the live store, so anything that rewrites `models.json` or `auth.json` (a Pi restart, a provider refresh, a `pi -p` probe) while a worker is in flight costs that attempt. Operational rule for the remaining waves: no headless `pi`, no provider probe, and no Pi restart between `op=dispatch` and `op=collect`; probe routes only while no worker is in flight.
- Thinker attempt 2 (agent `dg_run-9b53_thinker_9c236893`, tab `wK:t9`, pane `wK:p9`, 128 tool calls, ~30 min) authored a real 42 KB report with 19 evidence claims; `op=collect` settled clean: `processExitCode 0`, `terminalKind completed`, `identityMatches true`, `providerLinksVerified true`, `cleanupVerified true`, `agentFsViolationCount 0`, `discardedReadOnlyChanges 0`, `ledgerValid true` (`PASS: audited 1 ledger files`), `reportSha256 7c0c12ad609a62bebd39580e4cf17a6bb670c5dfcb9948d2f2c18614af790a98`, verdict `READY`. It verified the PRD's `a7054d7`-era findings against the current tree and added the ones later waves depend on: 53 entry points (39 both-copy, 11 installed-only, 3 missing, 2 dev-path-only); installed vs repository drift is 96 identical / 71 differing / 3 repo-only / 223 installed-only, with `job/ats-automation` installed-only; PATH `python3` is 3.9.6 and cannot run the scorer while `$HOME/.brew/bin/python3` 3.14.7 can; `uv` is referenced by none of the seven `SKILL.md` files; only `search-linkedin-jobs.mjs` uses `cdp-lease.mjs`/`retry-policy.mjs`, so `batch-fetch-jds.mjs`, `direct-jd-fetch.mjs` and `visible-listing-extract.mjs` navigate with no lease, no budget and no shared retry policy; `--skip-preflight` skips both gates; `scripts/jh-retry.sh` and `scripts/jh-apply-lib.sh` do not exist; `score_jobs_inline.py` has no `--linkedin-csv`/`--jobs-csv` flags (its real contract is seven `SCORE_*` variables plus one JSON output); the salary pipeline is `scripts/lib/enrich-pipeline.mjs`, a pure library that delegates live fetching to `sources/linkedin.mjs:62`; `jh_meta` has 0 rows so no `source_access:linkedin` key exists, and neither `job_fetch_state` nor `job_sponsorship_observations` exists; the canonical DB and its backup cannot be opened `mode=ro` (sqlite error 14 from the WAL sibling) and that read-only handle also rejects `.backup`, while `immutable=1` reads 3308 rows. Collision confirmation for the ownership map: US-003, US-005 and US-009 all write `jh-migrate.mjs`, and US-008 collides with the supervisor on `package.json` plus `docs/dependency-matrix.md` if it adds a CSV parser.
- The plan report is staged at `agent-output/linkedin-research-safety/US-001/us001-plan-report.json` (gitignored) so later workers can read the authority instead of having the supervisor paraphrase it. Implementer op `op_521f037b-1557-4777-8206-90c2058dfdbc` owns the five US-001 artifacts plus `test/research-baseline.test.mjs`. The plan's five slices were merged into one owner because they are one chain, not five independents: the checksum manifest digest feeds `baseline-check.json`, and the test asserts over all five artifacts, so parallel owners would race on each other's unfinished files.
- Implementer attempt 1 (agent `dg_run-9b53_implement_e1de961c`, tab `wK:tA`) failed the same way the thinker did but for a different reason: 60 tool calls and 102,935 characters of thinking, then a 37-character final message `Retrying (attempt 1/3, waiting 2s)...` and exit 0 with no report and no overlay file. The provider retried underneath and the Pi session died holding the retry. Its `op=collect` then hit a removed attempt directory (`command failed (2): .../agentfs-export.ts`), which classifies `unclassified` and parked the run at `awaiting_user`; `op=resolve decision=retry` returned it to `pending` for attempt 2. Recorded at the time: if a later attempt dies the same way, split US-001 into per-artifact slices. Attempt 2 died differently, and the real cause is recorded below.
- Implementer attempt 2 (agent `dg_run-9b53_implement_1dbb5b8c`, tab `wK:tB`) did the work properly: 35 minutes, all six artifacts present in the overlay, then a real 47,290-byte report with 27 claims and zero projection claims, verdict `DONE`, asserting all three US-001 acceptance criteria literally satisfied, `npm test` at 77 tests / 76 pass / 1 skip, and one release-gate finding reported upward (`npm run verify:release` fails on a pre-existing `scripts/` absolute-path hit, not on anything US-001 wrote). `op=collect` still recorded it `failed` with the same `command failed (2): .../agentfs-export.ts`, and cleanup then removed `acpx/dg_run-9b53_implement_1dbb5b8c/` including the overlay, so all six artifacts were destroyed and only the report survived.
- **Root cause, reproduced in isolation, and it is a supervisor prompt defect, not an extension bug.** `runExport()` in `scripts/agentfs-export.ts` returns 2 without exporting whenever `auditAgentFsChanges()` reports a violation, and my dispatch prompt told the worker to run `git status --porcelain` as a proof command. A plain `git status` refreshes the index, so `.git/index` becomes an overlay change that is not in `ownedPaths`; `platformMetadata()` in `lib/agentfs-sandbox.ts` filters only `._*` and `.DS_Store`, not git index writes. Minimal reproduction: sandbox over a scratch git repo with `ownedPaths=[owned.txt]`, worker running `echo mnt > owned.txt; git status --porcelain`, which writes `export-result.json` with `violations: [{path: ".git/index", kind: "file"}]` and `exported: false` at exit 2; the same sandbox with no git command exports at exit 0. Read-only nodes escape this because `discardAllChanges=true` moves every change into `ignored` and skips the violation check, which is exactly why the identical `git status` instruction in the Thinker prompt was harmless.
- Dispatch rule for every remaining wave, from this incident: an implementer prompt must never contain a git, `npm install`, or any other command that writes a repository path outside the slice's `ownedPaths`. Use `node --test test/<file>` instead of `npm test`, capture `git status` and before/after checksums host-side in the supervisor session before and after dispatch rather than asking the worker to prove them, and state in the prompt that any file the worker creates in the repository outside its owned paths destroys the settlement, with scratch work confined to `mktemp -d`.
- `run_9b5379bb-95e7-4989-b10b-7636b8a538f8` is abandoned after that second loss (`op=resolve decision=abort`: operation `cancelled`, run `cancelled`), and wave 1 re-initializes against the same frozen contracts because the accepted plan report is durable at `agent-output/linkedin-research-safety/US-001/us001-plan-report.json` and only the slice text carries the defect. What this run established and what still stands: the route findings, the provider-link and session-restart hazard, the projection-refusal behavior, the accepted 19-claim US-001 plan with its verified inventory facts, and the reproduced export rule. What it did not produce: any US-001 artifact on the host.
- Wave 1 retry 2 (run `run_12fb8e07-5526-49cb-8f48-41139a30d929`, implement op `op_56ddf14f-e632-40e5-a557-860c6d42bb8c`, agent `dg_run-12fb_implement_4740d913`, tab `wK:tD`, pane `wK:pD`, model `mtplx/mtplx-flash-next`) produced a third, different failure: 24 minutes, 16 tool calls, exactly one artifact (`checksum-manifest.txt`) present in the overlay, then exit 0 with a 637-byte `Supervisor projection` report and zero assistant text. `op=collect` classified it `worker-report-missing` and refused it, so the resilience gate held: no empty `DONE` advanced the graph.
- Reading the three implement failures together: `alibaba/qwen3.8-flash` died mid-provider-retry with 60 tool calls, `alibaba/qwen3.8-flash` then completed all six artifacts in 35 minutes and was destroyed by the supervisor's `git status` instruction, and the local `mtplx/mtplx-flash-next` route progressed 16 calls in 24 minutes and stopped with no report. The local route is the owner's deliberate cost choice made hours earlier and it has a 131 K window, which is the binding constraint for these prompts (a 29.7 KB contract plus a 42 KB plan report plus tool output), so it is not a defect to route around unilaterally.
- Context-budget amendment proved the diagnosis: after rewriting only the supervisor-owned contract file (the op task points at `agent-output/linkedin-research-safety/US-001/us001-slice-task.md`, so a slice's instructions can be corrected between attempts without re-planning or touching the owner's routing), the same local route authored all six artifacts and a real 27,023-byte report in 24 minutes, and `op=collect` settled clean for the first writable time: `agentFsViolationCount 0`, `exported true`, `cleanupVerified true`, `ledgerValid true`, `providerLinksVerified true`, report sha256 `ee28571d30cf3f440e75bdbec8ef2616b8f77449956912b99db2a94d9c8bb432`, verdict `DONE`. The six files are on the host and the supervisor re-verified independently: `node --test test/research-baseline.test.mjs` is 8 tests / 8 pass / 0 fail / 0 skipped, all six caller-inventory markers are present, the manifest hashes to `eead31cb082eb71a6c66729227ea582a69bf5a7a17c0a7fc67e0ecaef825238b`, `baseline-check.json` reports `linkedinRequests 0` with equal before/after digests and `integrityCheck ok`, `runtime-inventory.json` classifies 53 entry points, and `baseline.json` holds 20 rows.
- Review node (`op_186ed00c`, agent `dg_run-12fb_reviewer_b485b2eb`, tab `wK:tF`, `alibaba/qwen3.8-max`, report sha256 `262352c92b340fe77a6b2e2ce8bedf275f4a8f5577fdd42ae240daff071dec5b`) returned `PASS` with 13 claims after re-hashing all 89 recorded hash pairs (89 matching, 0 missing) and recomputing the 400-line manifest byte-identically. It also raised three things the implementer had not: the contract's own prove command 1 contradicts the frozen digest convention, `runtime-inventory.json` contains no `caller`/`spawn`/`relationship`/`decision`/`evidence` field at all, and the observed-execution entries are transcriptions rather than re-runs. Because the review op's task text is store-generated and cannot be replaced, the supervisor's lever for review context is a brief inside the artifacts directory, which is where `REVIEW-BRIEF.md` came from.
- Test node (`op_950b9794`, agent `dg_run-12fb_tester_c3f1a75d`, tab `wK:tG`, report sha256 `87d9ca653e93f9f1c759de3a81f0287da44479ec73bc121e63971c736abd17cc`) returned `NOT_OK` with 18 verified claims, and the loop earned its keep. Two of its findings are supervisor-verified in the host session: `node scripts/check-release-safety.mjs` fails with `[FAIL] forbidden-content: test/research-baseline.test.mjs - contains hardcoded-user-path`, so US-001's own guard test broke the release gate rather than inheriting a pre-existing failure (the tester proved the check passes on the committed tree and still fails with the plan of record restored), and `npm test` is 76/76 green, not the implementer's self-reported 77/76/1-skip. Its third finding stands unrefuted: acceptance criterion 1 names `runtime-inventory.json` as the carrier of caller relationships and drift decisions, and that artifact does not carry them, so the criterion holds only in substance via `caller-inventory.md`.
- Round 2 is dispatched against the same slice with a five-item fix list appended to the contract file (`SUPERVISOR AMENDMENT 2`): derive the interpreter assertion from `os.homedir()` instead of a literal home path, add `callerRelationships`, `driftDecision`, `evidenceReferences` per entry point plus a top-level `drift` block to `runtime-inventory.json`, assert those fields in the guard test, assert `missing.length === 0` so the drift check cannot pass vacuously on a machine without the absolute paths, and restate the two self-reports that did not reproduce. The supervisor scrubbed its own plan-of-record path literals to `$HOME` form, which took `check-release-safety.mjs` from two findings to one; the remaining finding is the worker's to fix.
- Ownership hard rule (amendment 3) resolved the second export refusal: `runExport` exit 2 came from an unowned overlay change, and the offender was reachable through my own frozen task text, which names `git status --porcelain` while describing the before-state. Amendment 3 forbade git, npm, brief-file edits and in-repo scratch by name and required the worker to list every file it wrote. The next attempt exported cleanly: `agentFsViolationCount 0`, `exported true`, `cleanupVerified true`, `ledgerValid true`, report sha256 `96fc4c4ebf1e179be7e3911db84f93e40c747f13d326135a70a274a157781a5d`, verdict `DONE`. All five round-2 fixes were then verified by the supervisor in the host session, not on the worker's word: `node scripts/check-release-safety.mjs` reports no findings, `runtime-inventory.json` carries `callerRelationships`, `driftDecision` and `evidenceReferences` on every one of the 53 entry points plus a top-level `drift` block, and the guard test went from 8 to 10 assertions, 10/10 passing, 0 skipped.
- Round 2 review (`op_f78601ed`, agent `dg_run-12fb_reviewer_0844f6f2`, tab `wK:tK`, report sha256 `24e462a2b593f935f28455fd3c5555298e882bb031fdaa4e9e425a5f2da99b49`) returned `PASS` after independently re-hashing all 89 recorded hash pairs, reproducing the 96/71/3/223 drift totals from the live trees, and confirming the rewritten interpreter assertion still proves the fact instead of being weakened. It also caught two things the implementer had not disclosed, and this is the part that justified keeping the review node: two installed-copy `callerRelationships` cite `classifySearchOutcome` message lines 310 and 317 instead of the real installed spawn sites 380 and 395, and `missingEntrypointNotes` asserts `direct-jd-fetch.mjs` is installed-only when the repository copy exists, is git-tracked, and is hashed by the manifest itself. The supervisor reproduced both against the live tree: `skills/linkedin-job-search/scripts/direct-jd-fetch.mjs` exists at 6965 bytes and `missingEntrypointNotes[1]` still claims otherwise.
- Round 2 test (`op_8804324c`, agent `dg_run-12fb_tester_8ca6407b`, tab `wK:tM`, report sha256 `b4e80efceae17d7eee8e9b430b6dc41774d9764f070bd073daf6f7b5618ae888`) returned `NOT_OK` again, but on a narrower and better-grounded basis than round 1: the three round-1 fixes are confirmed real, `npm test` is 78/78 green, `verify:release` exits 0, the manifest re-hash branch is non-vacuous, and criteria 2 and 3 hold. What failed is that both content defects survive and no assertion in the repository can catch either, which the tester proved by mutation: in a `mktemp -d` copy it rewrote all four jh-search citations to a fabricated line and deleted `missingEntrypointNotes` outright, and the guard test still reported 10/10 pass. So the suite validates the shape of a citation, never its truth.
- Round 3, the graph's final fix round, is dispatched with a three-item list (amendment 4 in the contract file): correct the two spawn citations to 380 and 395 with argument construction at 201 and 384, restate the false installed-only note and re-check the other two notes against the filesystem, and make citation truth machine-checkable by adding structured `citations: [{tree, path, lines: [[start, end]]}]` to each relationship plus `existsInRepo` / `existsInstalled` to each note, and asserting in the guard test that cited files exist, that cited ranges fit the file, that `spawned-by` ranges actually contain a launch token, and that the note booleans agree with `existsSync`. The worker must also demonstrate the new assertions bite by fabricating inside a temp copy only. This closes the gap the mutation test exposed rather than just fixing the two instances of it.
- Extension finding for the pi-agent-wave repo (not this repo's scope, recorded here because it fired during wave 1): a queued "Delegate Graph retry 1/3 is due" notice arrived for `run_12fb8e07` / `op_a4d3e465` naming model `alibaba/qwen3.8-max` and "attempt 1/4", while that operation was actually `running` with `transient_attempts=0` on `mtplx/mtplx-flash-next` and its worker process alive; `alibaba/qwen3.8-max` is the model of the *reviewer* agent that completed just before it. Acting on that notice would have redispatched a live attempt. Operational rule held: verify a retry notice against `op=status` or the operations row before acting, and treat `classifier_reason`/`retry_not_before` as the authority on whether a retry is due.
- Round 3 attempt 1 was refused by the same exporter a third time (`command failed (2): .../agentfs-export.ts`, `classifier_reason=unclassified`, run parked at `awaiting_user`, `current_node=implement round=3`). Unlike round 2, the retained diagnostics cannot name the offending path: the failure bundle keeps only the last 20 events and they are all thought chunks, and `export-result.json` lives inside the attempt directory that the failure path deletes. The supervisor verified the damage on the host instead: the round 2 artifacts are intact and unchanged (`runtime-inventory.json` 129,200 bytes, `checksum-manifest.txt` 62,632 bytes), `node --test test/research-baseline.test.mjs` is 10/10, `npm test` is 78 tests / 78 pass / 0 fail / 0 skipped, `node scripts/check-release-safety.mjs` reports no findings, and all three round-3 items are still outstanding, i.e. the false `direct-jd-fetch.mjs` installed-only note is still true-to-file-false, the two `310`/`317` spawn citations are still wrong, and no `citations` field exists anywhere.
- US-001 standing state, as at 2026-09-07T01:05Z, host-verified rather than worker-claimed: acceptance criteria 2 and 3 are satisfied in letter and independently reproduced (20-row digest-only slice with backup digest `750254e5300ee7804867c255c3d7957e42a44d969725a321b12d9b785d1bd2a7`, `integrityCheck ok`, manifest digest `eead31cb082eb71a6c66729227ea582a69bf5a7a17c0a7fc67e0ecaef825238b` unchanged before and after, all 400 manifest paths re-hashing clean, zero writes to the repository or installed trees, zero published home-absolute paths). Criterion 1 is satisfied in letter for structure (per-entry `callerRelationships`, `driftDecision`, `evidenceReferences`, `referencedAt` on all 53 entry points, top-level `drift` block reproducing 96/71/3/223, all 89 recorded hashes true) but carries two verified-false citation strings and one verified-false classification note, which no assertion in the suite can detect. The story is therefore not closable yet, and the two errata are recorded here so they cannot be lost.
- Recovery decision: the user selected a retry with the story directory plus the test file as owned paths. This widens ownership; it does not establish the cause of previous export failures. The earlier claim that only Alibaba produced successful writable settlements was incorrect: the local route also settled successfully, as recorded above. Provider choice and context exhaustion remain unproven explanations for missing reports.
- Directory-ownership trial: `run_06d8ec3e-f26c-4007-a865-dd0a41f7a2db`, implement operation `op_c471e935-b5bc-4558-a864-7f5f65f2ea3b`, initial attempt on `mtplx/mtplx-flash-next`, Herdr tab `wK:tQ`. The supervisor preserved the worker stream, export configuration, and before-state under `agent-output/linkedin-research-safety/attempt4-diagnostics.RLgcSO/` before collection. A read-only call to `auditAgentFsChanges()` found exactly two changes, both owned: `runtime-inventory.json` and `test/research-baseline.test.mjs`; no ownership violations. The files exported and the host independently ran the guard: 15 tests passed, zero failures/skips. The release-safety check passed; all five protected supervisor/reviewer files retained their recorded hashes. This demonstrates a successful export, not proof that widened ownership caused the improvement.
- Settlement remains incomplete: collection recorded `provider-link-churn` because `$HOME/.pi/agent/models-store.json` changed; the cause is unknown. The worker's report is an execution-only projection, not semantic evidence. US-001 remains unchecked pending authored evidence, review, testing, and audit. Recovery attempt 1 was scoped to verification and report authoring only: the accumulated implementation instructions were replaced with a compact contract, and the before-state was retained without changing provider routing.
- Recovery attempt 1 (`dg-implementer-0-1-98c36848de09`, tab `wK:tR`) also exited without an authored report. Collection refused export and parked the run at `awaiting_user`. No further retry was dispatched. The supervisor retained this attempt's worker stream and before-state at `agent-output/linkedin-research-safety/recovery1-diagnostics.dwgRjW/` before collection.
- Reproduced exporter defect, not a guessed ownership violation: `auditAgentFsChanges()` flagged the preserved `attempt4-diagnostics.RLgcSO/worker.stdout.ndjson` as an unowned change. Direct reads proved host and overlay both contain 5,320,565 bytes with SHA-256 `de2a47208efb7c07c6d2224878305a36be53133f1a3ec6b92c93796b0c3914d7` and mode `0600`. The actual Node `spawnSync` comparison fails with `ENOBUFS` under its default output buffer; the identical command with `maxBuffer=8388608` exits 0 and is byte-equal to the host. `agentFsChanges()` treats a failed comparison as a changed file. The evidence is in `recovery1-diagnostics.dwgRjW/exporter-finding.json`. This explains this attempt's false-positive export refusal only, not earlier unknown failures or the missing report. The extension was not edited.
- Current product state: the initial directory-ownership export remains the candidate under review, with corrected spawn citations and the `direct-jd-fetch.mjs` counterpart classified as `both`. The supervisor ran `npm test`: 83 passed, zero failures/skips; `npm run verify:release` exited 0. US-001 is still incomplete because semantic completion, independent review/testing, and the final audit have not settled. The user subsequently authorized fixing the exporter in its own repository. That narrow repair was committed as `4839976`, merged into the extension's `main`, and pushed: both content-reading subprocesses remove the default capture cap, with real large-file tests proving unchanged unowned files are accepted, changed unowned files remain rejected, and large owned files export intact. The extension's targeted tests passed (7, zero skips); its full Node suite reported 396 passed, 11 skipped, zero failures, and its typecheck/release dry runs passed. The missing semantic report remains an independent blocker; no evidence gate or global model routing was changed.
- Verification-only split, attempt 1: the user authorized proceeding after the exporter repair. Retire the failed implementation run rather than replay its frozen rebuild instructions. A fresh build graph will verify the existing US-001 artifacts with one small evidence-producing slice, using the configured `test` tier for this verification-only work. Source scripts, the baseline artifacts, and the guard test remain read-only; only a new private verification-output directory is owned. Keep the normal review, test, and audit nodes. This changes the execution approach, not the acceptance criteria or the default coding route. If verification uncovers a product/evidence defect, record it before planning a separate correction; do not silently widen this slice. Restore point for this supervisor-log edit: Git object `32f46fa10834c1742b98359263d9104cc605c6d6`.
- Verification-only split, attempt 1 did not reach implementation: planning operation `op_96fa6aa6-8ff7-419a-9c4d-f9ab91805e6e` in `run_77169608-0880-46c5-a08d-6b4c5c84cb41` produced no assistant/tool activity. Its attempt-local Pi session recorded HTTP 429 `insufficient_quota`: Alibaba's weekly token plan was exhausted, with reset text `09-10 12:45:00 UTC`. Collection classified the missing report as `unclassified` and parked the run rather than advancing the frozen fallback chain. Evidence: `agent-output/linkedin-research-safety/us001-verification-provider-blocker.json`. The configured test tier also promoted the graph's implementation node back to its coding capability floor, so it did not actually select a test model for that node.
- Verification-only split, attempt 2: retire the quota-blocked run and explicitly select `openai-codex/gpt-6-astra`, already present in the planning/review fallback chain, for this evidence-only graph. This is a per-run override to avoid repeating a confirmed exhausted provider; it does not alter global routing or authorize coding, source/baseline changes, or bypass any report/review/test/audit gate. Codex's current quota was not assumed available; the dispatched planning task ran and authored a `READY` report, preserved as `agent-output/linkedin-research-safety/us001-verification-plan.json`. It independently observed 15 passing guard tests, zero skips, release-safety exit 0, and current artifact hashes; it explicitly did not claim complete semantic acceptance.
- The Codex plan's first collection reached successful report/export/provider/ledger checks, then failed `ownedProcessesAbsent` during cleanup. A subsequent process inspection found no matching processes. Recollection returned cached settlement without cleanup proof, so the supervisor reran the existing `delegate_core.verify_cleanup_absence()` against the same attempt and completed only the interrupted settlement-finalization steps from `command_wait()`. The fresh audit reported every absence check true and no owned process matches, preserving report SHA-256 `95192871ed13dbfd69af0ea75c61e25ac62d27a3b79a53362c7f8a8fd517213f`. No worker was rerun and no evidence gate was waived. Resume the same operation through the public recovery control and record that authenticated report.
- Dispatch normalization for the verification slice: map the planner's `owns` key to the graph's `ownedPaths` field. Remove its copied planning-only clause demanding zero repository exports, because it contradicts the already approved task to export new verification evidence under the sole owned directory. No source, test, baseline, or ownership scope changes result from that normalization.
- The verification slice authored a `DONE` report and exported `agent-output/linkedin-research-safety/US-001-verification/verification.json`; collection verified cleanup, provider links, ownership and ledger evidence without manual recovery. The supervisor rehashed the five current artifacts and matched every before/after digest. The worker recorded 15 passing guard tests, zero skips, and release-safety exit 0. Semantic US-001 acceptance remains pending independent review.
- Review handoff correction: the extension's generated review/test/audit tasks are generic and do not embed the originating task's safety constraints or evidence paths. Before dispatching those roles, add a short project `AGENTS.md` pointing to this issue, the current verification plan/evidence, and the current review brief, with the no-LinkedIn and read-only-review boundaries explicit. This supervisor-owned project guidance is part of the execution handoff, not a product behavior change; it adds no new acceptance criterion.
- Independent review `op_591553f0-e5a2-4f64-bf6f-a03a0f73dfec` returned `FAIL` with report SHA-256 `1bbfe4eccdb069c0b8e623a9f0f4aded0f7b78362b29920d1b2ba769705aac44`, preserved as `agent-output/linkedin-research-safety/us001-review-current.json`. The supervisor confirmed two remaining semantic inventory defects: the score_jobs_inline.py drift summary uses test-score_jobs_inline.py measurements and falsely says the scorer differs; the scorer's subprocess.run edge to role-classifier-cli.mjs is typed as imports. The reviewer reproduced all selected baseline identity, description, score and salary-provenance records from the real saved backup, but the recorded URI recipe required an adjustment on a disposable copy. No acceptance checkbox is advanced by these partial results.
- Mechanical evidence correction, attempt 1: supervisor owns only `agent-output/linkedin-research-safety/US-001/runtime-inventory.json`, `agent-output/linkedin-research-safety/US-001/baseline-check.json`, and `test/research-baseline.test.mjs` for this correction. Add focused failing checks named `byte-identical drift summaries agree with entrypoint measurements` and `scorer classifier edge records a subprocess launch`; correct the two metadata records and their exact source citations. Supplement, rather than rewrite, the historical baseline recipe with a newly verified procedure that changes journal mode only on the disposable copy before read-only replay. Prove it on the existing bounded real backup, printing only hashes/counts. Preserve the original files before editing. No production scripts, canonical workspace, installed skills, or routing files are writable. Then rerun the existing evidence-only graph slice and independent review; its worker still owns only the separate verification directory. This is a separate supervisor correction, not an expanded assignment to the verification worker.
- Mechanical correction, attempt 1 completed pending independent re-review: the two new consistency checks failed against the old metadata, then passed after the scorer drift identity and launch edge were corrected. Guard result: 17 passed, zero failures/skips; full `npm test`: 85 passed, zero failures/skips; `npm run verify:release`: exit 0. Original files are retained in `agent-output/linkedin-research-safety/us001-correction.mJoBgk/`. The new `replay.json` records an actual SQLite backup into disposable scratch, output-only journal-mode conversion, successful URI `mode=ro` reopen and integrity check, and matching identities/descriptions/original-score arrays/salary-provenance arrays for all 20 selected records. The recorded backup and its sidecars remained byte-identical and scratch was removed. `baseline-check.json` now links this fresh reproduction as a separate supervisor supplement; its historical acquisition record and the original baseline data are retained.
- Re-review `op_3829d657-2743-4453-be27-a273bba0b67c` confirmed the concrete corrections and the fresh backup replay but returned `FAIL` on a broader original US-001 requirement: the search-linkedin-jobs.mjs decision classifies its drift from import lines alone, omitting installed knownLocations configuration, company/location splitting, and detail top-card resolution changes. The supervisor reproduced those added functions/configuration references in the actual diff. The report is retained as `agent-output/linkedin-research-safety/us001-review-after-correction.json` (SHA-256 `6a4815a33603e9d27b4f0335447436b04fb7340e8ffe20e70e09b1fee06ea4cc`). Criteria 2 and 3 have independent supporting evidence, but US-001 remains open pending complete drift reconciliation and final test/audit settlement.
- Drift-analysis split, attempt 1: stop the repetitive verification loop. Audit every non-identical paired entrypoint (17, extracted from the current inventory) in a separate read-only research graph, partitioned into LinkedIn extraction/backfill/tests, salary, and orchestration/storage. Use the already proven Codex fallback without altering global coding routes. Each source pair needs concrete static diff evidence, its runtime/configuration/portability/behavioral or accepted nonfunctional classification, and an explicit preserve/port/retain decision with the relevant planned story or an explained no-change decision. Import-line heuristics alone and generic unassigned deferrals do not satisfy this step. Workers own no repository paths; they author private reports. The supervisor owns integration into runtime-inventory.json and caller-inventory.md, updates derived classification summaries mechanically, and keeps production scripts unchanged. Only after this bounded analysis and integration may the parent verification graph re-run its existing checks and independent acceptance gates.
- All three drift researchers authored reports covering the input exactly; the supervisor matched every current source hash and validated the reported ranges under the existing guard's newline-splitting convention (including a terminal empty line). The salary report encountered the same cleanup-absence failure; rerunning the extension's official absence audit found no owned processes and passed every check, after which the same already executed attempt was reattached and settled without a new worker launch.
- Report-schema correction: the supervisor incorrectly requested extra top-level `slices`/`findings` fields. The validator permits only `schemaVersion`, `verdict`, `claims`, and the operational `execution` field. Collection's report repair converted the worker output to canonical claims, so pre-collection copies and settled-report hashes are not interchangeable. Preserve both as separately hashed artifacts: raw structured output is candidate integration data; `*-settled.json` is the accepted semantic report. The earlier research-plan payload's planSource path pointed at raw `plan.json` while its hash referred to the settled report; the collected-report manifest explicitly corrects that binding. The analysis scopes were unchanged and already coverage-checked. Synthesis must use the settled claims as evidence, check raw candidates against those claims/source, and encode each consolidated structured finding as a JSON string in an allowed evidence.detail field, never as an extra top-level report key.
- Static synthesis authored and settled with SHA-256 `b7e104382f560dc18055a6221c3969f784e0ecadee5c73077056bf04008f6fd1`, preserved at `agent-output/linkedin-research-safety/US-001-drift/synthesis.json`. It contains 17 structured findings in the supported evidence.detail format, distinguishes verified static observations from proposed future reconciliation, and does not claim parent-story completion. Integration, attempt 1: preserve current inventory/caller/test files, add the focused guard `paired drift decisions match the grounded reconciliation report` and demonstrate it fails before integration. Replace only the paired entries' driftDecision data from these grounded findings, bind each decision to the settled report hash and source-pair identity, mechanically recompute the classification summary, and add the reconciliation section to caller-inventory.md. Keep all other inventory data and every production script unchanged. The same parent verification worker then rechecks current artifacts; independent review, test and audit remain mandatory.
- Drift integration, attempt 1: the new grounded-decision guard failed before integration and passed afterward. All 17 paired decisions now match the settled synthesis by source identity and report hash; unrelated inventory data was mechanically checked unchanged, the classification counts still total 53 entries, and caller-inventory.md contains the corresponding reconciliation table. The guard passed 18 tests and the full suite passed 86 tests with no skips/failures. Full release verification exposed a separate publication-scope defect: the profile-leak checker recursively scans ignored, untracked private `agent-output/` evidence. Git confirmed this directory is ignored and has no tracked files. It reported profile-field matches in the required private research records; no values were printed.

### Publication-scope correction (authorized)

**Status:** Complete, attempt 1; independent review, testing and final audit passed alongside US-001. The user explicitly chose to retain the verified reconciliation and correct the publication scope instead of deleting evidence or weakening checks on publishable files. Restore point: clean target files at HEAD `f52e41936243c02ce093b06a618a25f998e76f4a`; tracked-work snapshot `453d754fbd65f3ee5e4e8ccf63876aefff014d84`.

**Approach and ownership:** Supervisor owns `scripts/check-local-profile-leaks.mjs`, `test/local-profile-leaks.test.mjs`, and the matching section of `docs/security-and-privacy.md`. At an actual Git worktree root, enumerate tracked files plus non-ignored untracked files using Git's NUL-delimited listing; tracked ignored files must remain included. For standalone export directories, or failed Git enumeration, retain conservative full-directory scanning and existing explicit exceptions. Preserve sensitive-value extraction and redacted diagnostics. Runtime skill sources, installed skills and canonical workspace data remain unchanged. No commits or pushes are authorized by this correction.

**Acceptance:**
- [x] `ignores untracked Git-ignored evidence but rejects publishable leaks` failed before the fix and passed after it using a real temporary Git repository and real files. Proof: targeted run changed from 8 passed / 1 failed to 9 passed / 0 failed, with zero skips.
- [x] `rejects profile leaks in tracked ignored files` passed with a force-added ignored file in a temporary repository; no source-path exemption hides it. Proof: the targeted Node test detected `agent-output/evidence.md` despite its ignore rule.
- [x] `scans complete non-Git export directories` and `falls back to full scanning when Git enumeration fails` passed; the latter used a genuinely corrupt temporary Git index, not a fake Git implementation. Proof: both returned a finding for the private-value fixture.
- [x] Existing profile-leak tests and the full suite passed. Proof: `npm test` reported 90 passed, zero failures/skips; the actual maintainer-worktree `npm run verify:release` exited 0 and its profile gate checked 157 sensitive values with no exact matches, not SKIP. `git diff --check` passed. `docs/security-and-privacy.md` now describes Git publication candidates, tracked ignored files, standalone exports and conservative enumeration-failure fallback.

Final acceptance checkpoint: independent review `op_0960ec2b-a08f-4813-abf6-4f2c6111b6bf` passed (report SHA-256 `a446d84547f48757dfed87f483cf8eeab52b15e763e9fbcb904671f7a89757d0`), and independent testing `op_7d4d9b7b-c117-437b-a0ba-40948df4e3b1` returned GREEN (SHA-256 `c8104f140d08ba18688e7cb9c408a30118a9893cc0b2e794f489c35f04da8b59`). The tester reproduced the real backup slice and intended mutation failures, passed 90 root tests and every release component with the profile check active. Project-level evidence for the 13 settled authored reports was appended using the bundled ledger writer, preserving operation identities and original agent registration timestamps; its audit passed. Missing-report/quota failures remain operational diagnostics, not fabricated semantic reports. The fresh-context final audit passed with SHA-256 `4807cba69c1a4bb8c6cb99a94e9d4ba5ca6b0cb914706777d848f549d4247b98`; its final ledger entry was appended and the complete 14-entry ledger audit passed. A subsequent host check passed all 90 tests without skips, all release components with the profile check active, and `git diff --check`. No runtime skill source, canonical job data or installed skill was changed. No production rollout or application submission is claimed. Before beginning US-002 source changes, make the acquisition-only baseline checkpoint explicitly phase-scoped rather than treating expected later source changes as baseline failures.

### Acquisition-checkpoint separation

**Status:** Verified, attempt 1. Supervisor-owned mechanical preparation for the next wave; include the small preparation diff in US-002's independent review. Restore point: tracked-work snapshot `d1afeb789efaee4005b370821ccd82bd949210c9`; preserve the untracked guard separately before moving it.

Move the strict acquisition guard from `test/research-baseline.test.mjs` to **new** `scripts/check-research-baseline.mjs`, keeping every assertion and its repository-relative root resolution. Add `verify:research-baseline` to `package.json` as an explicit Node test invocation. Document that it requires the private evidence and original runtime-source state; ordinary regression tests must remain usable after intentional runtime edits and in public clones. Do not regenerate, weaken or silently skip acquisition assertions, rewrite historical reports, copy whole source trees, or change runtime skills. Supervisor owns the move, package entry, README and current agent guidance. Historical report paths continue to identify their original execution revision.

- [x] The explicit checkpoint passed all existing 18 checks before runtime edits, with zero skips; byte comparison against `us001-correction.mJoBgk/checkpoint-before-move.mjs` proved only its invocation comment changed.
- [x] `npm test` passed 73 regression tests without skips and `npm run verify:release` passed with the maintainer profile check active. A real temporary publication export, with no private baseline and an isolated workspace override, also passed both commands; see `agent-output/linkedin-research-safety/checkpoint-separation-export.json`. The export's unavailable profile cache is explicitly skipped, not a privacy verification result. No acquisition assertion was changed or skipped.
- [x] README's Development checks section and AGENTS.md's Acquisition checkpoint section distinguish acquisition verification from later behavior regression tests. No immutable US-001 artifact or ledger entry was edited.

Checkpoint move verification: all 18 acquisition checks and 72 ordinary tests passed, with zero skips. Byte comparison proved that only the invocation comment changed. Release verification then exposed an existing documentation-parser defect: `npm run verify:research-baseline` and `npm run verify:release` are truncated to `verify` by the script-name regex. The new README section was restored to its prior state; the independently passing move was retained. Before restoring those documentation lines, extend the parser's existing name character class to include colon, without adding aliases or suppressing validation. Supervisor also owns the clean tracked `scripts/check-doc-commands.mjs` and `test/doc-checks.test.mjs` for this mechanical correction.
- [x] `resolves complete namespaced npm script names` proved a defined colon-bearing name resolves, while a missing colon-bearing name is not accepted through its defined prefix. The targeted suite changed from 11 passed / 1 failed to 12 passed / 0 failed, without skips. The parser correction added only colon to its existing name character class.


### File ownership and handoff rules

- The supervisor owns this PRD, the ownership map, common dependency manifests, and shared documentation integration. Workers report requested changes to those paths rather than editing them concurrently. The supervisor records verified story progress and evidence here as each slice completes.
- Give `jh-migrate.mjs` one owner at a time. In the conservative schedule its ownership passes through US-003, US-009, then US-005. Each successor preserves and reruns tests for its predecessors' schema changes.
- Keep `jh-search.mjs`, `search-linkedin-jobs.mjs`, and shared collection helpers within the sequential safety/cache track. Every dispatch prompt must list exact writable paths; all non-owned paths are read-only. Reconcile any additional shared caller found in US-001 before dispatch.
- US-003 hands `test/research-access.test.mjs` to US-004. US-008 hands `test/research-sponsorship.test.mjs` to US-009. US-009 hands `test/research-report.test.mjs` to US-010. Handoffs include the verified artifact revision and passing-test evidence; the prior worker must have stopped writing.
- US-010 owns `jh-report-gate.mjs` integration. Salary, CV, and sponsorship workers supply the agreed data contracts and fixtures instead of independently editing the combined renderer/gate.
- Give each worker its own temporary SQLite copy, test-output directory, and local test target. The canonical workspace DB, browser session, and installed skill tree are not parallel writable resources. No worker may access LinkedIn as part of investigation or offline verification.
- If a worker discovers a dependency on another worker's unfinished change or needs a non-owned file, it reports the conflict. The supervisor updates the plan and serializes the affected work; workers do not silently expand ownership or mark blocked acceptance criteria complete.

Each story is a reviewable slice. If a slice cannot fit one focused session, split it here before implementation. Keep attempt count beside the story's progress entry; after repeated failures, simplify the slice rather than repeatedly rebuilding verification infrastructure.

### Processing flow

1. Load explicit user scope and source-access state.
2. If LinkedIn is paused, mark discovery deferred and use existing saved rows; do not run the exact-URL preflight.
3. If collection is permitted operationally, acquire the shared LinkedIn owner, plan/checkpoint queries, and admit only required jobs-page navigations through the same gate.
4. Classify every result before extracting/persisting a JD. A block pauses the source; a usable posting updates its content and fetch metadata transactionally.
5. CV scoring and cached salary/sponsorship extraction operate on saved content. External enrichment is separate, source-scoped, and cannot redirect back into paused LinkedIn.
6. Render saved results and explicit coverage gaps; no report action submits an application.

### Frozen source-access contract (US-003 foundation)

Accepted planning report: `US-003/plan.json`, SHA-256 `bd0c03752f82828041d8c02a98d96791634f2600715e0123f024e3fb64c4e180`. These are design decisions, not implemented behavior. The sole authoritative new key is `source.linkedin.access`; this supersedes the proposed `source_access:linkedin`, without reinterpreting or modifying an unrelated existing old key. A lease cannot waive the pause, and a resume cannot waive the lease/budget.

**API.** Export synchronous `LINKEDIN_ACCESS_KEY`, `readLinkedInAccess(dbPath)`, `pauseLinkedInAccess(dbPath, { reason, runId = null })`, and `resumeLinkedInAccess(dbPath, { reason, acknowledge = false })`. Each API requires an explicit nonempty filesystem path; reject whitespace-only, NUL, `:memory:` and `file:` URI values. There is no API canonical fallback or injectable store. Missing/invalid options return failure instead of accidental TypeError. Imports perform no DB/dependency loading, filesystem writes, output, exit or navigation. Resolve `better-sqlite3` lazily through the migration's `createRequire` pattern at `JOBHUNTER_HOME` or `HOME`/`homedir()` plus `.job-hunter`; do not alter the shared resolver or eagerly import WebSocket dependencies.

**Results.** All APIs return exactly `{ok,allowed,record,error}`. Success has `ok:true`, the validated record, `error:null`, and `allowed` true only for ready. Failure has `ok:false`, `allowed:false`, `record:null` and `{code,message}`. Fixed pairs: `INVALID_ARGUMENT` / `Invalid LinkedIn access arguments`; `MISSING_STATE` / `LinkedIn access state is missing`; `INVALID_STATE` / `LinkedIn access state is invalid`; `STORAGE_ERROR` / `LinkedIn access storage is unavailable`. Missing file/table/key is missing; malformed JSON/record is invalid; permissions, invalid SQLite, incompatible table shape, dependency loading, locking and read/write/commit failures are storage errors. Distinguish absence from inaccessible paths via filesystem error codes and table absence via `sqlite_master`. Never disclose raw inputs, paths, SQL, stacks or native errors. A valid paused read is successful but disallowed.

**Record.** Writers emit all fields below. Reads allow omitted `runId`, normalizing it to null. Reject unknown fields, missing required fields, arrays/null, unsupported versions and coercion.

| Field | Frozen validation |
|---|---|
| `schemaVersion` | Numeric `1` |
| `state` | Exactly `paused` or `ready` |
| `reason` | 1–240 UTF-16 code units; no U+0000–001F or U+007F. Trim input once; persisted text must already be trimmed. |
| `observedAt` | Canonical UTC ISO with milliseconds, valid Date parsing and exact `toISOString()` round-trip. |
| `runId` | Null, omitted on reads, or `/^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/`; ready requires null. |
| `operatorConfirmation` | Paused requires null; ready requires exactly `{confirmedAt,reason}`, equal to `observedAt` and top-level `reason`. |

Reasons are intended non-secret operational summaries; length bounds do not detect secrets. Fabricated ready JSON is not proof of actual human review. No TTL or date-driven transition exists.

**Read and transitions.** Reads use `{readonly:true,fileMustExist:true,timeout:1000}` and close in `finally`. No initialization, repair, timestamp change, immutable mode, journal changes or mutating pragmas; missing-path reads create neither file nor parent. Pause/resume validate inputs before opening existing storage with `{fileMustExist:true,timeout:1000}`. An actual immediate transaction encloses validation/read/update/commit, requiring an existing valid record; failures leave missing/corrupt state untouched. Pause always writes fresh paused state, supplied reason/runId and null confirmation, including repeated pauses. Resume requires `acknowledge === true` and valid reason, then writes fresh ready state, null runId and matching confirmation, including repeated resumes. Update `jh_meta.updated_at` only on committed writes; return success after commit, roll back failures and close handles. Transaction order serializes transitions but does not coordinate navigation or prioritize pause over a later explicit resume.

**Migration.** Retain exports, default-path behavior, output/return fields, taxonomy and stage/backfill semantics. Enclose all changes in one immediate transaction and close in `finally`. Insert only an absent key, using an explicit key-conflict policy, with `{schemaVersion:1,state:'paused',reason:'unreviewed installation',observedAt:new Date().toISOString(),runId:null,operatorConfirmation:null}`. Preserve exact existing value/updated_at bytes for ready, paused, malformed and unsupported rows without parsing or repair. Add `linkedinAccessInitialized:boolean` to the result. Prove whole-migration rollback, including legacy schema work and access insertion, under a deliberate SQLite error.

**CLI.** Grammar: `status [--db PATH]`; `pause --reason TEXT [--run-id ID] [--db PATH]`; `resume --acknowledge --reason TEXT [--db PATH]`; standalone `--help`/`-h`. Database precedence: `--db`, `JOBHUNTER_DB`, selected workspace's `jobhunter.sqlite`. Require one action; reject duplicate, unknown or action-inappropriate flags, missing values, extra positionals and a supplied value after `--acknowledge`. Export pure `parseArgs(argv)` returning `{help,action,db,reason,runId,acknowledge}`, null for inapplicable fields, and `main(argv = process.argv.slice(2))` returning an exit integer; guarded direct invocation sets `process.exitCode`. Imports/help do not load SQLite or touch storage. Emit one API-result JSON line on stdout for operational results, including storage failures. Valid paused status and successful transitions exit 0; operational/record failures exit 2. Invalid input/usage exits 1, stdout empty, fixed non-secret stderr diagnostic. Help exits 0 with usage only. No force, repair, implicit acknowledgment, timeout unpause, JSON-file import or browser action.

**Proof detail.** The named foundation tests use real external SQLite and actual CLI/API subprocesses. Cover inert import/help with missing dependencies and DB; unchanged state, timestamps and schema on status; every record field and malformed/missing storage; sanitized argument errors; cross-process and repeated transitions; omitted-runId compatibility; and old timestamps without automatic resume. Exercise real trigger-induced update rollback and a separate process holding `BEGIN IMMEDIATE`, synchronized by pipe/IPC, with successful recovery after release. Use deterministic real write rejection, not a mock store or ineffective permission test. Migration proof preserves every existing state form and unrelated keys/timestamps, retains taxonomy and one-time backfill behavior, and proves rollback. Every test database, including default-path CLI fixtures, must be disposable; only dependency resolution may read the installed workspace.

### Proposed job-fetch metadata

Add **new** `job_fetch_state` via the existing migration entrypoint:

- Composite primary/foreign key: `(source, job_id)` references `jobs(source, job_id)`.
- `content_hash`: hash of normalized stored description, nullable for legacy rows until calculated locally.
- `fetched_at`: nullable UTC timestamp of a successful actual fetch, never inferred from `updated_at`.
- `fetched_url`: actual successful document URL, nullable for legacy data.
- `last_attempt_at`: most recent attempted refresh, separately tracked.
- `last_outcome`: allowed set `success`, `failed`, `blocked`, `deferred`; nullable before a new attempt.
- `last_reason`: bounded diagnostic without credentials or full page contents.

Successful content replacement and fetch metadata commit together. A failed/blocked attempt updates only attempt/outcome diagnostics and preserves the last successful content/hash/timestamp. Missing metadata does not mean missing content.

Do not add an automatically expiring job cache in this slice. Explicit refresh IDs remain supported; any scheduled age policy must name its scope and freshness tradeoff in this plan before implementation. Search checkpoints, rather than a second query database, own same-scan deduplication and resume outcomes.

### Proposed CV-evidence contract

Use the existing DOCX extraction path and score-result storage. For each assessed criterion keep: normalized requirement, exact JD quotation, mandatory/preferred classification, `matched`/`not_evidenced`/`unknown` status, and CV text span or explicit cache key/value evidence reference. Store sensitive source references privately; do not copy full CV text into public tests or external requests.

A vocabulary/alias map recognizes terms only. It must not infer possession, years of experience, or transferable skills. Recompute the count/percentage relationship from those criterion records immediately before persistence and rendering. Preserve current score-run identity conventions while adding the evidence-version invalidation needed for correctness.

### Proposed sponsor-register cache and job observations

Manifest under `$JOBHUNTER_HOME/sponsor-register/`:

- `publicationUrl`, `csvUrl`, `fetchedAt`, `publicationUpdatedAt` when available;
- `sha256`, `byteCount`, `rowCount`, `headers`, and parser/normalizer version;
- retained immutable snapshot filename and a pointer to the validated current snapshot.

Keep the snapshot referenced by historical job evidence. Fetch failures leave the last validated snapshot readable but explicitly stale. No hardcoded dated URL, permanent assumptions about column order, or company-name fuzzy auto-approval.

Add **new** `job_sponsorship_observations` with a surrogate observation ID, `(source, job_id)` foreign key, observation timestamp, and:

| Field | Allowed meaning |
|---|---|
| `employer_status` | `confirmed_skilled_worker`, `ambiguous`, `no_confirmed_match`, `unknown`. |
| `role_status` | `explicit_yes`, `explicit_no`, `conditional`, `unknown`, `conflicting`. |
| `sponsorship_need` | `required`, `not_required`, `unknown`, from explicit country-specific user data. |
| `evidence_json` | Validated array of source URL, fetched/observed time, quotation or snapshot hash/row identity, applicability, and source kind. |
| `reason` | Explanation of matching/ambiguity and any role/employer conflict. |
| `input_hash` | Identity of JD, employer mapping, snapshot, and relevant user-cache inputs, allowing unchanged-input reuse. |

Keep raw register legal name and route in the evidence, not only a normalized match key. Unknown is not false. These are local research-state vocabularies, not a new shared cross-domain enum framework. If implementation introduces Python classes, classify their domain role and declare applicable existing Protocols before coding.

### Output and compatibility contract

Use the existing seven presentation columns: `Fit / Category | Role | Salary / provenance | Posting age | Reason / gaps | Location / source | URL`. Preserve `Search ID` and the `Full facts` labels: Scope, Filters, Pipeline, Salary, Source status, Scoring basis, Artifacts.

For UK rows, put a concise sponsorship summary in `Reason / gaps`, with full evidence in per-row details/JSON. The JSON row adds separate employer/role/need fields. Existing consumers must still accept the old fields; version the new envelope explicitly rather than changing meanings silently.

Report scope must distinguish:
- all requested live source runs completed;
- source coverage partial but saved-data analysis completed;
- missing descriptions/evidence preventing assessment.

Do not claim `best-paying company` from market estimates. Report `highest comparable posted pay among collected jobs`, with currency, compensation basis, location, sample count, and collection time. Recalculate all counts and aggregates in code from the displayed/underlying rows.

### Testing and release commands

Existing commands verified from the repository; these are to be run during implementation, not claimed as run during planning:

```bash
npm test
npm run verify:release
node skills/job-hunter/scripts/test-jh-migrate.mjs
node skills/job-hunter/scripts/test-jh-search.mjs
node skills/job-hunter/scripts/test-jh-report-gate.mjs
node skills/job-hunter/scripts/test-jh-freshness.mjs
node skills/linkedin-job-search/scripts/test-page-state.mjs
node skills/linkedin-job-search/scripts/test-cdp-lease.mjs
node skills/linkedin-job-search/scripts/test-retry-policy.mjs
node skills/linkedin-job-search/scripts/test-search-linkedin-jobs.mjs
node skills/linkedin-job-search/scripts/test-extraction-and-filter.mjs
python3 skills/job-match-scorer/scripts/test-score_jobs_inline.py
node skills/salary-calculator/scripts/test-itjobswatch-parse.mjs
node scripts/install.mjs --dry-run
```

The new `test/research-*.test.mjs` files run through the existing root `node --test test/*.test.mjs` command. They must not require the user's live LinkedIn session or private CV. Real private-data integration is the explicit rehearsal, not the default published test suite.

Use real temporary SQLite/filesystem/processes and the actual installed DOCX parser for invocable local dependencies. Bind a fake navigation endpoint to loopback for block/launch testing, with explicit destination binding so a fixture cannot accidentally redirect to LinkedIn. Rehearse exact persisted command artifacts end-to-end before live authorization: argument boundaries, assignment timing, launch-before-log ordering, file descriptors, and propagation of the producer's exit status through pipelines. Syntax checks alone are insufficient.

Existing `npm run verify:release` gates include release safety, local-profile leaks, skill closure, dependency matrix, documentation commands, and required documentation sections. New published skill files must use workspace/environment resolution rather than machine-specific absolute paths.

### Deployment and rollback gates

- No current LinkedIn account access is authorized by this plan.
- No plan criterion depends on provoking another warning or measuring an enforcement threshold.
- Before editing: record clean tracked HEAD; preserve dirty/untracked or non-git-covered state appropriately. Before DB changes: make a proper SQLite backup.
- Source changes belong in this repository's `skills/` tree. Port only individually reviewed installed differences; never bulk-sync either direction.
- Do not declare deployment complete until installed file hashes, dependency resolution, offline rehearsal, and persisted pause state are separately verified.
- Commits, pushes, PR creation, installation, and future live canaries require the user's separate request. Merge only on request.

## 7. Success Metrics

These are product measurements, not predicted results. Compute them from evidence artifacts rather than estimating them:

- **Unchanged-data LinkedIn access:** zero actual LinkedIn fetch/navigation attempts during the real offline analysis pass, proved by the network boundary and caller trace.
- **Restriction admission behavior:** no new covered navigation admitted after the first block; separately report already-sent/in-flight activity and cleanup outcome.
- **Duplicate work:** compare successful new-ID navigations with unique required new IDs and explicitly authorized refreshes. List any extra request and reason; do not claim a percentage reduction without a measured comparable baseline.
- **Scope conservation:** requested queries and discovered IDs equal the disjoint union of reported outcome buckets; deferred work remains recoverable.
- **CV grounding:** every matched criterion has runtime evidence, and every displayed score recomputes from its criterion counts.
- **Salary quality:** each selected pay observation has source/provenance; company/role comparisons disclose compatible basis and sample count.
- **Sponsorship quality:** each resolved UK outcome has role/register evidence, and unknown cases remain visible. No target requiring fabricated coverage.
- **Operational result:** after separately authorized normal use, observed warnings are recorded, but a quiet observation window is not proof of future immunity or permitted access.

## 8. Open Questions

No additional product question is needed to write this plan: the user already confirmed preserving discovery, CV matching, salary research, UK sponsorship, cache reuse, and stopping on restrictions.

The following are implementation/operational gates, not permission to guess:

1. Which installed launchers, scheduled tasks, extensions, or browser activity actually contributed to this incident? Resolve from read-only evidence in US-001; absent telemetry stays unknown.
2. Which installed-only changes must be ported before deployment? Record exact file-level decisions in US-001 before any installer write.
3. What source access is available/permitted after the restriction clears? No approved LinkedIn API/export entitlement was verified during research. Do not promise one or claim caching changes LinkedIn's terms.
4. What operational scan cadence and shared budget does the owner want after manual review? Preserve full scope through resumable batches; no undocumented safe threshold or automatic restart.
5. What is the score table's compatible representation for insufficient evidence? Verify schema constraints and record the decision in US-006 before changing persistence.
6. Which employer aliases, if any, can the owner confirm, and what sponsor-register freshness policy should apply? Until confirmed, retain ambiguity/staleness and continue showing the job for review.
7. Does a future live LinkedIn canary remain necessary and authorized? It is explicitly not part of current offline acceptance, and live compatibility must remain marked unverified until that separate step succeeds.

## 9. Reverification and completion authorization

**S01 review correction, direct revision 2:** The independent synthesis settled and reproduced a concrete safety gap: an existing `/checkpoint/` target with title `LinkedIn` was classified healthy from metadata, admitting another probe. The supervisor corrected this by conservatively classifying an observed LinkedIn checkpoint URL as blocked when its existing canonical classification is not already restrictive, without evaluating that non-job page or changing the shared classifier. The real loopback/SQLite generic-title checkpoint proof fails before the fix (`resume-job-work/S01-review-red.log`, exit 0 versus expected 3) and passes afterward. Added wrapper doctor/CDP failure and failed-pause propagation assertions and an Indeed-only test with a LinkedIn checkpoint tab actually present. These changes remain in the original owned preflight/test files; no schema or shared framework change is needed.

Review provenance follow-up: standalone/preflight pauses currently retain the foundation's valid nullable `runId`; wrapper child-result pauses have their wrapper run ID. Propagating wrapper identity into preflight requires an additional caller contract and is deferred to later caller/owner-context integration, not silently added to S01. Record this limit for full US-003 acceptance. Review gap C is handled as a coverage gap: `accessError` is assigned only after a canonical observation whose pause write failed; admission failure uses `admitted:false`. The final auditor must rerun the current targeted tests and judge this corrected revision; the source and synthesis reports remain original revision-1 evidence, not retroactively rewritten. Current revision-2 evidence is `resume-job-work/S01-revision2-hashes.json`, `S01-revision2.diff` and `S01-revision2-checks.json`. Fresh targeted tests report 11 passing, root regression 99 passing, and release/syntax/whitespace checks pass, with zero skips. Original revision-1 hashes/reports remain unchanged. The final auditor authored PASS and independently reran the current targeted suite: retained `S01-direct-verification/reports/audit-access.stdout` proves 11 passing tests, zero failures/skips, with empty stderr. The supervisor rechecked all revision-2 source hashes and unchanged non-S01 first-wave files. Public recording completed verification run `run_d06014b4-a328-452c-8906-1b1279698376` at terminal/PASS, confirmed by a separate status read. The complete product-verification ledger audit passes all three entries. S01 revision 2 is accepted as a bounded wrapper/preflight slice, with the provenance and worker-boundary notes below retained; this does not complete full US-003.

**Audit boundary finding:** The final report lists Git status inspection despite the no-Git worker instruction, and the transport receipt records one discarded read-only change, zero export violations and verified cleanup. I don't know which path was discarded from the retained receipt. No auditor code changes were imported; the actual product files match the audited revision-2 hashes. This is a retained worker-boundary finding, not a claim that the worker made zero filesystem changes or blanket acceptance of its compliance statement. Scoped code/test findings are adjudicated separately from that deviation.

**S01 accepted delivery history:** Direct implementation and local checks pass; independent product verification is run `run_d06014b4-a328-452c-8906-1b1279698376` under auto routing, with no coding worker. Its source operation has now settled and been recorded DONE: the actual env/Node command passed 11 tests, identifiers/artifact paths validated, source hashes matched, and cleanup/export integrity checks passed. `S01-direct-verification/collected-source.json` binds the retained authored report and settlement evidence. The revision-1 source and synthesis observations are settled. Revision 2 corrects the reproduced checkpoint gap and adds the requested negative-path coverage; the final independent audit of that current revision is settled and recorded PASS. Source operation `op_89aeeea6-4d4e-4d0e-a243-03a6f73bf8a0` owns only `agent-output/linkedin-research-safety/S01-direct-verification/source/` and its assigned private report; all runtime files, tests, this issue and docs are read-only for that worker. Synthesis and final audit own only private reports and scratch. They must read `agent-output/linkedin-research-safety/S01-direct-verification/BRIEF.md`. The supervisor owns coordination artifacts and the scoped diff/hashes in `resume-job-work/S01-direct.diff` and `S01-source-hashes.json`. Keep the repository frozen during each worker. Do not claim whole US-003 or accept unimplemented S07a/S04 from these proofs.

**Current direct S01 execution:** The user selected “Implement directly here (recommended)” after the delegated attempt limit. Build `run_29a6f045-1941-4c37-96ac-9bcfbfa1c763` was cancelled through public `resolve/abort`; its failed attempts stay historical and will not be reopened or presented as successful worker reports. The supervisor now implements S01 directly in its existing three owned files, retaining all accepted foundation behavior and the existing proof names. This is an explicit execution-route change, not another unchanged delegate attempt. Independent product review and verification remain required.

Implementation clarifications for S01: an explicit set of Indeed-only probe URLs takes the existing Indeed path without inspecting LinkedIn tabs or depending on LinkedIn access state; empty, mixed or other probe sets remain LinkedIn-gated. This preserves Indeed-only behavior while closing the LinkedIn bypass. Reject disallowed LinkedIn probe destinations before any CDP contact. Existing member/non-job tabs are not evaluated; canonical restrictions identifiable from target URL/title can still stop the run. A canonical observation stops subsequent evaluation, probes and heartbeats, persists the pause, and permits only closure of the probe target created by this invocation. A canonical terminal summary also takes precedence over a nominal child exit zero. The wrapper propagates a canonical pause or admission denial from preflight as blocked; ordinary doctor/CDP failures retain their preflight-failed classification. If pause persistence fails, return a sanitized not-ok result rather than claiming a persisted pause.

The proof harness invokes actual wrapper/preflight subprocesses, real disposable SQLite, and a loopback HTTP/WebSocket CDP fixture. A Node preload sentinel handles only the out-of-scope doctor/collector children so no browser or job site can be contacted; it does not replace either entrypoint under test. Imports use an explicitly bound loopback port. Keep the original eight foundation tests intact apart from extending the inert-import list and its safe subprocess harness. Test-only temporary homes may link the installed dependency directory read-only. No live site, canonical database, installed-skill, schema, collector or framework edits are authorized.

**Historical product resumption:** The user retired the blocked framework-verification worker and then explicitly requested returning to LinkedIn research-safety work. This supersedes the temporary requirement below to finish a separate framework-verification graph before product retries; it neither accepts its rejected reports nor waives this PRD's review, test and final semantic-audit gates. Do not reopen that retired worker or start another framework-repair project. The catalog correction has local passing evidence in pi-agent-wave-new-design; no broader framework acceptance is claimed.

Historical directive (superseded): this section previously directed resuming build `run_29a6f045-1941-4c37-96ac-9bcfbfa1c763` by dispatching one implementer at a time (S01, then S07a, then S04) through public resolve/dispatch/collect/record operations. That build was cancelled through public `resolve/abort` before any serialization-route worker started; it is terminal and must not be resumed, and its per-attempt budgets and failure evidence remain authoritative history. The accepted S01, S07a and S04 ownership/API/proof contracts below remain the planning basis for direct supervisor implementation. Any future delegated execution is a fresh run with refreshed evidence, not a revival of that build or its blocked operations.

**Delegation-repair verification (2026-09-11, closed):** combined-revision acceptance of the delegation repair (writable-worker export/parked-sibling settlement repair plus the later mutable-provider-catalog correction) was attempted through fresh operations-graph runs under auto routing with unchanged digest `dc4e3cace60972d9159be133e92e8611ded2893fc64abdec9a55d0990a0d28ba`. The first attempt `run_ac97518b-e42f-4346-9c63-df72300b8d4a` aborted on command shape (its worker validly reported 26 credential/mount-dependent failures from the full-glob suite and was aborted before any retry). The second attempt `run_1e0af10e-313e-4164-88ec-6841ffd24f8e` completed the full chain (source → synthesis → final audit) on a narrowed environment-neutral five-file argv; its final audit verdict is **FAIL scoped to evidence packaging** — the repair mechanisms were judged present, with the mutable catalog exercised live by every dispatch including the audit session itself — and the run closed at `blocked` with no acceptance recorded. Open findings (contract unreachable for worker roles, duplicate-dispatch session identity, ledger supplement, plan-naming) plus all evidence are recorded in `agent-output/worker-export-recovery-verification-3/ACCEPTANCE.md`. This remains framework verification only: it neither completes US-003, S07a, S04 or any product story nor proves live compatibility. Runs `run_54099ff8`, `run_68bcd1ec` and `run_ac97518b` stay cancelled and un-reopened; the verification-2 directory is historical and its `source/` artifacts are earlier-revision evidence whose snapshots predate the catalog correction.

**Fresh baseline:** `agent-output/linkedin-research-safety/resume-job-work/checks.json` and adjacent logs show access 8/8, backfill 15/15 and regression 96/96 passing, with zero skips; migration, release and whitespace checks exit zero. All first-wave owned files still match the pre-attempt fingerprints. Preserve accepted US-001/US-002 and the accepted US-003 state/migration/operator foundation. None of the missing caller, scoring, lease, salary, sponsorship or reporting behavior is accepted by these baseline results.

**Progress:** S01 attempt 2 failed before export: the worker still supplied only an execution projection after its bounded report repair. Original diagnostics/report are retained in `resume-job-work/S01-attempt2/`; all first-wave owned files still match the restore hashes. The graph scheduled its first same-model transient retry under the frozen auto policy. S01 attempt 3 then failed with `ACPX worker failed: exit=0 terminal=None`; it produced no report and exported no source changes. Its original diagnostics are retained in `resume-job-work/S01-attempt3/`. The supervisor recorded S01 and the build as blocked at the documented three-attempt limit. No fourth unchanged attempt is authorized; a simpler/split scope or explicit user-approved execution-route change is required. S07a and S04 have not been redispatched during this serialized resumption. No new product candidate has been imported. The restore point and exact current file copies are in `resume-job-work/restore.json` and `resume-job-work/restore/`. Mark each planned criterion only after its actual evidence and required review gates pass; do not round a bounded slice into whole-story completion.

2026-09-10: The user authorized inspecting the actual checkout, verifying implementation against this PRD, and implementing missing work. This does not authorize LinkedIn access, deployment, installation, canonical-data or installed-skill writes, commits, pushes, or pull requests. The user's reported pi-agent-wave-new-design repairs are not product acceptance evidence and do not expand this repository's scope.

Reverification status: **source audits settled; the attempt-1 delegated build is cancelled; S01 is accepted as a bounded direct slice; the remaining first-wave slices are unaccepted**. Read-only graph `run_6bcc380d-20e6-43e2-918d-2cbcbe01b3ba` audits each story and criterion, identifies concrete gaps, and proposes bounded implementation slices. It uses the previously authorized exact Codex route and visible Herdr workers. US-001/US-002 remain accepted historical work unless a concrete current defect is found. The US-003 foundation's unsettled historical audit remains unaccepted until fresh evidence and settlement close it; full caller integration is not implied by foundation correctness.

Supervisor will first run current offline repository, release, access, migration and backfill checks, retain command/exit/output evidence, and compare findings with the audit. Record accepted slice scope and file ownership here before runtime edits. Preserve the PRD's existing proof names, per-criterion checkboxes, and migration ownership order; record any necessary changes before implementing them. During worker execution, mounted repository files remain frozen. Progress and evidence are recorded after settlement, and accepted slices require independent review/testing and fresh semantic audit.

Restore point: HEAD `f52e41936243c02ce093b06a618a25f998e76f4a`, tracked-work snapshot `50047ac069abdd0d64630672fbf1a9114f19e92e`, with dirty plan/project instructions and untracked source/test backups in `agent-output/linkedin-research-safety/reverification-restore-vutnegd8/restore.json`. Historical failed/blocked attempts remain evidence, not successful retries.

Fresh supervisor verification: `agent-output/linkedin-research-safety/reverification/host-checks/` retains access (8 passing tests), backfill (15 passing tests), migration PASS, root suite (96 passing tests), full release verification and diff checks; every command exited 0. These establish current tested behavior, not missing caller coverage or whole-PRD completion. The settled read-only planner report at `reverification/plan.json` proposes slices S00–S17; proposals remain unaccepted implementation plans until their specific scope is recorded here. Independent source audits now cover (a) US-001–US-005 and bounded foundation acceptance, (b) US-006–US-007 including real scorer schema/evidence and salary paths, and (c) US-008–US-011 including dependency contracts, report gaps and offline proof prerequisites. All source workers own no repository paths and author only their private reports; synthesis must reconcile their concrete findings before implementation.

### Auto-policy continuation

The user's explicit `/delegate --policy auto` instruction supersedes the earlier exact-Codex selection for all new work. Build run `run_29a6f045-1941-4c37-96ac-9bcfbfa1c763` has the frozen auto policy digest `dc4e3cace60972d9159be133e92e8611ded2893fc64abdec9a55d0990a0d28ba`. Do not change global routing or substitute a hand-selected model. Its initial task `continue` means continue the user's verification-and-implementation request under this PRD and the current reverification brief.

The former research run's planner and all three source audits settled successfully with report hashes and cleanup evidence retained in `reverification/collected-reports.json`; its unlaunched synthesis was cancelled after the policy switch. This is not a claim that the research graph reached full acceptance. `delegate-ledger audit linkedin-research-safety-reverification` passes four entries. New planning must synthesize the collected audits rather than repeat their entire investigation.

The US-003 state/migration/operator foundation is now accepted as a bounded delivered slice: `reverification/access-audit.json` independently verifies all three foundation criteria with real disposable SQLite/API/CLI tests, fresh syntax checks and source fingerprints, plus the supervisor's current root/release evidence. Its settlement and cleanup are verified in the adjacent `access-audit-settlement.json` and `access-audit-cleanup.json`. This is fresh acceptance, not retroactive acceptance of the older unsettled PASS. Full US-003 remains partial; caller admission, restriction propagation and URL rejection are not implemented.

Historical first implementation wave status: **blocked on delegate execution; attempt 1**. The current serialized resumption above supersedes this status. The auto-policy thinker will freeze bounded, disjoint first-wave slices for wrapper/preflight admission, CV-evidence scoring correction, and strict lease/retry primitives. Existing behavior and proof names in the PRD remain authoritative. The source audit corrected planner mistakes: a keepalive test name is not a production LinkedIn caller; two scorer files alone cannot establish persistence/caller compatibility; the salary pipeline has an actual parser-to-writer contract mismatch and saved-content bypass. Record concrete signatures, owned paths and limitations before dispatch. Later caller/cache integration, schema changes, salary/sponsorship/reporting and offline rehearsal remain required; passing the first wave must not close their story checkboxes.

US-006 persistence decision, verified against the real production schema in `reverification/scoring-salary-audit.json`: preserve current constraints rather than invent a new stretch enum or nullable score. An unassessable row uses `fit_score=0`, `cta=Skip`, `stretch_label=Blocked`, zero mandatory counts, empty matches and a versioned `assessment_status=insufficient_evidence` reason in existing evidence JSON. This numeric zero is a storage placeholder, not an assessed 0% match. Runtime output and the later research report must expose the unassessable status and must not present it as an assessed percentage. A scored assessment alone uses `matched / total * 100`. The thinker must preserve CV/job/cache/taxonomy provenance and separately specify any remaining persistence/caller ownership rather than claiming that the isolated scoring change completes US-006.

### Accepted first-wave implementation plan

The auto thinker settled READY with zero ownership violations/discards and verified cleanup. Its immutable full contract is `agent-output/linkedin-research-safety/reverification/auto-plan.json`, SHA-256 `d38e934a7080a0e391874906df9f7cb4b43026e934407436824f86e531dd374d`; `auto-plan-slices.json` is an exact extraction of its three named implementation-slice evidence objects. These contracts are accepted with the clarifications below. Restore files and pre-edit hashes are in `reverification/wave1-restore/manifest.json`. The supervisor owns this issue and shared documentation; every worker's non-owned path is read-only.

**S01 — wrapper/preflight admission; attempt 1.** Own only `skills/job-hunter/scripts/jh-search.mjs`, `skills/linkedin-job-search/scripts/cdp-preflight.mjs`, and `test/research-access.test.mjs`. Reuse unchanged `readLinkedInAccess(dbPath)` and `pauseLinkedInAccess(dbPath, {reason, runId})`. For the LinkedIn wrapper branch, check access before run-directory/checkpoint creation, doctor, child launch or CDP contact; `--skip-preflight` never bypasses admission. Preserve Indeed-only behavior. Propagate the same explicit database identity to preflight. Denied wrapper admission uses existing `EXIT.BLOCKED` (4); standalone preflight denial uses existing not-ok exit 3 and sanitized JSON. Persist canonical blocking observations before checkpoint/exit, including `login_required`, while preserving unrelated transient and Indeed semantics. Make preflight inert on import, export `parseArgs`, `researchNavigationDecision` and `main`, with explicit database precedence and guarded direct execution.

`researchNavigationDecision(url, {fixtureOrigins} = {})` returns allowed/code/reason; production permits only HTTPS LinkedIn job-search/detail paths and rejects credentials, non-job routes and other hosts. Explicit loopback fixtures are opt-in only, never a production alternate destination. Validate before target creation and terminate the preflight probe loop on its first canonical restriction. No shared navigation framework or new state schema belongs in this slice.

- [x] S01: `node --test test/research-access.test.mjs` preserves existing foundation proofs and adds the planned restart/admission, skip-preflight and member-profile rejection proofs using actual subprocesses and disposable SQLite. Evidence: `resume-job-work/S01-access-verified.log` reports 11 passing tests and zero skips, including the original eight names and the three required new names; `S01-direct-red.log` records the pre-fix failures. These tests cover wrapper/preflight only until later integration expands their entrypoint table; independent semantic acceptance is terminal/PASS for S01 revision 2; full US-003 remains incomplete.
- [x] S01: both edited runtime modules pass `node --check`; existing `test-jh-search.mjs` and `test-page-state.mjs` pass. Evidence: `resume-job-work/S01-direct-checks.json` names the successful syntax, wrapper and classifier logs; `S01-regression-verified.log` reports 99 passing tests and zero skips, and `S01-release-verified.log` passes the release gate. The wrapper fixture runs the actual published entrypoint from an external temporary home with read-only dependency links and forbidden-network/child sentinels; no canonical data was used. The initial release check rejected email-like text in synthetic credential URLs; the fixture now constructs credentials through URL setters, with the release guard unchanged. Independent semantic acceptance is terminal/PASS for S01 revision 2.

**S07a — isolated CV evidence; attempt 1.** Own only `skills/job-match-scorer/scripts/score_jobs_inline.py` and `skills/job-match-scorer/scripts/test-score_jobs_inline.py`. Extract real DOCX text once per pass, retain it, build one evidence object and pass it into scoring. Skill dictionaries may recognize requirements but never imply possession. Honor `SCORE_CACHE_PATH`, then the explicit canonical-workspace cache, then the working-directory fallback; invalid or absent cache values remain unknown. Remove static language/authorization possession assumptions. Mandatory criteria retain exact JD quotations and matched CV spans or explicit cache references, preferred criteria are excluded, and unknown mandatory criteria are not matched. Numeric scoring uses actual criterion counts. Preserve taxonomy classification and the existing application threshold. Apply the insufficient-evidence contract above; this slice does not create a new enum, schema migration, persistence writer or combined report.

- [x] S07a: the Python test file passes the PRD-named evidence cases (`test_only_cv_supported_skill_matches`, `test_preferred_skill_does_not_change_denominator`, `test_missing_evidence_is_unknown`, `test_equal_inputs_produce_equal_evidence`, `test_unassessable_job_is_not_a_fake_percentage`) plus preserved relevant tests, using the actual DOCX parser and real scorer. Evidence: direct invocation `python3 skills/job-match-scorer/scripts/test-score_jobs_inline.py` exits 0 with every named proof passing (`agent-output/linkedin-research-safety/S07a-scoring/test-run-direct.log`); fixtures are synthetic DOCX/cache built in temporary directories, and the end-to-end section runs the real scorer process with valid and missing `SCORE_CACHE_PATH`. Preserved table proofs keep classification labels, blocker carriage, leadership/data-gap facts and bridge parity; their old fixed-five-boolean numeric expectations were intentionally not reproduced.
- [x] S07a: Python compilation succeeds with no compilation artifacts left in the repository. Output records versioned CV/job/cache/taxonomy provenance, reproducible evidence and derived counts. Evidence: `py_compile` passes for both files; an initial run's `__pycache__` was caught by the release gate, removed, and the full `verify:release` chain then passed with only the two owned files modified. Each scored row carries `evidence_provenance` with CV/cache/taxonomy digests and scorer version `inline-v3`, plus derived mandatory/technical counts. Full US-006 remains unchecked until persistence/caller integration, the root CV-evidence test and private real-data recomputation are delivered.

**S04 — strict source ownership/retry primitives; attempt 1.** Own only `skills/linkedin-job-search/scripts/cdp-lease.mjs`, `skills/linkedin-job-search/scripts/retry-policy.mjs`, `skills/linkedin-job-search/scripts/test-cdp-lease.mjs`, and `skills/linkedin-job-search/scripts/test-retry-policy.mjs`. Extend existing components rather than create competing lock/budget storage. Preserve legacy non-strict callers; add explicit strict LinkedIn owner acquisition with required `dbPath`, one source-wide lease identity and one source-wide budget identity independent of CDP port. `acquireStrictLinkedInOwner` and `tryAcquireStrictLinkedInOwner` return a discriminated success/denial result; an owner rechecks persisted access when reserving requests, tracks ownership and tabs, and releases only its own lease. Strict storage corruption, contention, mutex failure and read/write/permission errors deny admission rather than warn and continue. Existing default request-window values are retained for compatibility, not advertised as safe platform limits.

Strict retry mode treats the first canonical block, rate limit, challenge or login-required observation as terminal for the source and exposes `RETRY_VERDICT.SOURCE_TERMINATED`; existing non-strict behavior is preserved. Honor cancellation and denied rechecks while waiting. Caller owner-context transfer and all-entrypoint coordination remain later slices, not implied by primitive tests.

- [x] S04: `test-cdp-lease.mjs` passes real-file/process proofs for strict cross-port exclusion, corrupt/busy/unwritable state, ownership-safe release and pause during waiting, while preserving legacy checks. Evidence: direct invocation `node skills/linkedin-job-search/scripts/test-cdp-lease.mjs` exits 0 with all sixteen original legacy proofs plus eleven new strict proofs passing — port-free one-identity contention (in-handle and real child-process exclusion naming the foreign holder pid), corrupt-lease/corrupt-budget/unwritable-lockDir denial, busy-mutex denial (no fail-open slot), ownership-safe release of an externally overwritten lease, acquisition-time pause denial, mid-wait pause abort, cancellation abort, shared source-wide budget continuity across owners, and the default persisted-access resolver exercised through a real module boundary with fail-closed behavior on unresolvable/erroring access state.
- [x] S04: `test-retry-policy.mjs` proves the first canonical restriction terminates strict retries; syntax checks pass. Full US-004 remains unchecked until production callers use the strict primitives and the PRD integration proofs pass. Evidence: five new strict proofs (first `blocked`/`active_challenge`/`rate_limited`/`login_required` observation terminates source-wide with `RETRY_VERDICT.SOURCE_TERMINATED`; strict termination dominates `isCircuitBroken`; per-key exhaustion retained; legacy `login_required` non-blocking behavior and policy independence preserved) pass on direct invocation; `node --check` passes for both modules and both unchanged production callers; `npm test` remains 99/99.

**First-wave acceptance:** all owned code, tests, comments and relevant help text must agree. The supervisor runs the ordinary suite and full release/privacy checks after exported changes, and records the actual output/exit status. Independent review, testing and a fresh semantic audit must accept these bounded slices. Keep every later-story criterion unchecked. If a slice needs another file or changed contract, stop that slice, amend this issue before ownership changes, then continue.

**Attempt 1 blocked:** all three auto-routed implementers selected `mtplx/mtplx-flash-next` from the frozen policy. Their retained reports are execution-only projections, not authored semantic reports. S01 and S07a additionally failed AgentFS export because of an unowned `.git/index` change. S04 was rejected for `worker-report-missing`; its verified cleanup does not make the projection acceptable. The graph is `awaiting_user` after the S07a export failure. S01's failed settlement could not be recorded once the sibling changed the run state, so its operation still reports running; do not interpret that as verified ongoing implementation or complete cleanup. Do not bypass ownership validation, import rejected candidate code, change model routes, or silently retry.

**S07a direct implementation (2026-09-11, awaiting independent review):** under the retained direct-supervisor authorization, `score_jobs_inline.py` was rewritten as `inline-v3` and `test-score_jobs_inline.py` rewritten against it; no schema, migration, persistence-writer, caller or installed-tree file changed. Contract: one evidence bundle is extracted per pass (real DOCX text, cache fields, taxonomy digest, scorer version) and passed into every `score_job`; mandatory criteria are extracted from actual JD sentences with exact quotations; a criterion counts as matched only from a word-bounded CV span or an explicit cache field — skill dictionaries recognize requirements but never imply possession, and the static `USER_SPOKEN` language assumption is removed; unknown mandatory criteria stay unknown and count in the denominator; `fit_score = matched / total × 100` from actual criterion counts; a job without assessable mandatory criteria uses the accepted placeholder contract (fit 0, Skip, Blocked, zero counts, in-band `assessment_status=insufficient_evidence` marker) rather than a fabricated percentage; `SCORE_CACHE_PATH` is honored with canonical-workspace and working-directory fallbacks, and an invalid explicit path yields no cache plus a stderr warning; preferred items never enter the mandatory denominator; unresolved mandatory language keeps the fail-safe blocker rule, and unknown authorization is never inferred into a blocker (only an explicit cache contradiction of a no-sponsorship requirement blocks). Derived search identity now includes CV bytes, cache bytes, taxonomy digest and scorer version, so changed evidence invalidates prior derived IDs; an explicit `SCORE_SEARCH_ID` is still preserved, which is what the unchanged orchestrator always supplies. Behavior changes to flag for review and documentation: English requirements now need positive CV/cache evidence instead of static possession, and unclear authorization no longer blocks. The strict `verify:research-baseline` checkpoint is by design run before intentional runtime edits; its manifest pins the pre-change hashes of both repository scorer paths, so a post-edit run will now report the two intended drifts — the manifest is before-research acquisition evidence and must not be updated to accommodate this change. Independent review/testing, the root CV-evidence test, persistence/caller integration proofs and private real-data recomputation remain.

**S04 direct implementation (2026-09-11, awaiting independent review):** under the same retained direct-supervisor authorization, `cdp-lease.mjs` gained `acquireStrictLinkedInOwner`/`tryAcquireStrictLinkedInOwner` and `retry-policy.mjs` gained `createStrictSourceRetryPolicy` with `RETRY_VERDICT.SOURCE_TERMINATED` and `STRICT_SOURCE_BLOCKING_STATES`; the two existing test files gained the required proofs. No production caller was switched: `search-linkedin-jobs.mjs` keeps the legacy warn-and-continue lease and the three-strike policy, and `search-indeed-jobs.mjs` is untouched — caller adoption is the later integration slice, so full US-004 stays unchecked. Design decisions for review: strict ownership is one opt-in object binding a required `dbPath`, a port-free lease identity and a port-free budget identity on the existing storage primitives (shared budget files continue across runs by design); persisted access is rechecked at acquisition, at every reserve entry and on each wait slice, and only an explicit ready state admits — missing, paused, invalid or unreadable access state, corrupt or unwritable storage, contention and mutex timeout all deny as discriminated results; the access reader is injectable, else resolves through `LINKEDIN_ACCESS_MODULE`, else the sibling access module, and a strictly unverifiable state denies admission. Derived from implementation, not observed in production: waiting-with-pause proofs are timing-bounded assertions on disposable fixtures; no live browser, lease or platform behavior was exercised.

**S08a — caller adoption of the strict primitives; attempt 1 (2026-09-11, implemented under the retained direct-supervisor authorization, awaiting independent review).** Owns only `skills/linkedin-job-search/scripts/search-linkedin-jobs.mjs`, `test-cdp-lease.mjs` and `test-retry-policy.mjs`; `cdp-lease.mjs` and `retry-policy.mjs` are not re-edited. Contract: `--strict-owner` (or `LINKEDIN_STRICT_OWNER=1`) switches the LinkedIn search caller to the strict primitives — source-owner admission runs before any browser/CDP contact with the workspace `dbPath` required (denial writes a `source_blocked` summary, defers the requested scope, exits 2 and never touches CDP); one source-wide strict retry policy shared by the search and detail phases terminates the run on the first canonical restriction, and in strict mode the policy is checked before every attempt including a detail fetch's first; each navigation reserves a slot through the owner immediately before sending, and a denial (pause, cancellation or storage failure) halts further admissions instead of the legacy fail-open; cleanup releases only an owned lease. Unflagged runs keep the legacy lease/budget/policy path byte-identical, so the step-2 fallback removal lands for the strict path first; flipping the default is a separate decision that waits for the collector end-to-end fixture. Destination binding: `LINKEDIN_TARGET_BASE` is accepted only for loopback hosts with `LINKEDIN_ALLOW_LOCAL_TARGET=1`; any other value aborts startup rather than silently navigating elsewhere. Explicitly not in this slice: wrapper/child ownership transfer (US-004 step 3), the collector end-to-end field-parity fixture, the `--stealth` browser-launch flag removal (US-004 step 6 — presence verified in both `search-linkedin-jobs.mjs` and `batch-fetch-jds.mjs` launch commands; removing it changes live browser behavior and needs its own decision), and any Indeed-path change. Required proofs: the three named `test-cdp-lease.mjs` cases run with real subprocesses, real filesystem and a loopback counting HTTP target, including one subcase that executes the unmodified `search-linkedin-jobs.mjs` as a subprocess; the `test-retry-policy.mjs` additions cover the shared source-wide termination and a bounded transient case. The SQLite-backed variants of the budget/access proofs stay open until a fixture with real disposable SQLite runs, so the second US-004 acceptance criterion cannot be checked from the file/process proofs alone.

**S08a verification (2026-09-11):** `test-cdp-lease.mjs` exits 0 with all 29 sections including the three new named proofs; `test-retry-policy.mjs` exits 0 including S6/S7; every other `skills/linkedin-job-search` direct suite exits 0 (run from its verified invocation directory — `test-save-to-sqlite.mjs` is path-sensitive and must run from the repository root, where it passes 8/8); `npm test` remains 99/99 with 0 skips; `verify:release` passes all six gates. Evidence: `agent-output/linkedin-research-safety/S08a-caller-admission/test-run-direct.log`. Implementation notes for review: `enforceRateLimit()` per-process pacing is retained under strict mode (the owner slot reservation happens in `admitNavigation()` immediately before dispatch, so no double counting — the legacy `sharedBudget` is never created in strict mode); the loopback-target subcases and the unverified-access subcase exercise the real CLI against no CDP endpoint, proving early termination, not successful loopback collection (collector field-parity under a rewritten navigation path remains its own open AC); strict success runs still consult `ensureObscura` after admission, so “admission before browser” is enforced by ordering (admission exits the process on denial), and the reviewer should challenge whether that ordering is sufficient. Working-tree restore point for this slice is archived as `agent-output/linkedin-research-safety/S08a-caller-admission/pre-S08a-snapshot.sha256` plus the pre-snapshot files beside it.

`reverification/wave1-failures/` retains original failure/projection/available settlement and cleanup files. `checkout-verification.json` freshly compares all nine owned files with `wave1-restore/manifest.json`: every file is unchanged, so no runtime implementation landed and no runtime restoration was necessary. The evidence ledger preserves the three rejected candidates and passes all eight current entries. No slice checkbox is satisfied by these attempts, and no final build semantic audit has run. I don't know what caused the `.git/index` writes or premature worker endings from the retained evidence; diagnosis or repair of delegation is separate work, not grounds to weaken this project's gates.

**User-authorized delegation repair:** the user selected investigating/repairing `pi-agent-wave-new-design`, then resuming this PRD. Its existing `tasks/prd-package-delegate-graph.md` now records the repair on branch `issue-worker-export-recovery`. Actual Git in AgentFS reproduces optional index refresh; the production worker environment now suppresses optional Git locks while deliberate unowned edits still fail export. Projection reports now enter the existing bounded report repair before export, and parallel sibling settlement/dispatch guards are corrected. Local repair verification passes: Node reports 462 passing tests, zero failures and 12 explicit opt-in skips; package Bun reports 46 passing; typecheck, installation rehearsal, dry-run packaging/publication, compilation and diff checks pass. This is repair evidence, not job-hunter feature acceptance.

Activation is now demonstrated: public recording of S01's retained failure succeeded, and the tool's recovery decision escalated this build to blocked. No product retry or dispatch occurred. The first repair verification run is cancelled with its incorrect report retained as rejected. Replacement auto run `run_68bcd1ec-a00d-4aa3-b970-8a0d46f6394e` settled and recorded a source report with correct graph identity, declared exported artifacts and actual output proving 37 passing tests. Its synthesis and independent semantic audit remain pending; their brief is `agent-output/worker-export-recovery-verification-2/BRIEF.md`. The repair's final acceptance and this build's recovery remain outstanding. The nine first-wave job-hunter files still have no accepted implementation changes. Do not restart product implementation or mark any first-wave criterion until these gates are resolved through public tools under auto routing.

**S09 — restriction seam consolidation and observation-site pause (2026-09-11, direct supervisor implementation, awaiting independent review).** Scope came from a design review of the branch. Changes: (1) `linkedin-page-state.mjs` now exports `RESTRICTION_STATES` (`active_challenge`, `blocked`, `rate_limited`, `login_required`), `isRestrictionState` and `researchNavigationDecision`; `retry-policy.mjs`, `cdp-preflight.mjs`, `jh-search.mjs` and the collector's exit-code set import them instead of keeping four diverging local copies (the collector's exit set previously omitted `login_required`). (2) The strict owner in `cdp-lease.mjs` resolves `pauseLinkedInAccess` beside the reader (injectable as `accessPauser`) and exposes `owner.pauseSource(reason)`; the collector calls it on the first restriction it observes in either phase, before release, and records the outcome in `summary.terminalStatuses.sourcePause`. (3) `batch-fetch-jds.mjs` reads the access state before any output or CDP probe (sanitized JSON line, exit 2), persists the pause synchronously when the first restriction is published, and validates every stored `jobs.url` against the jobs-route allowlist before a target exists; rejected destinations are counted as `excluded` with a reason and never written. (4) The collector validates every navigation URL at `admitNavigation(url)`, checks the retry policy before reserving a budget slot, and the strict owner's access recheck runs outside the budget mutex. (5) `linkedin-access.mjs` caches the SQLite binding per dependency home; `jh-common.mjs` falls back to `homedir()`; a relative `lockDir` is denied; the stray fixture-created `scripts/http:` directory was removed and the lease tests no longer use fixed `/tmp` database paths. Not in this slice: the strict default flip, wrapper/child ownership transfer, removal of the masking helpers, and splitting this execution log out of the PRD (recorded as a recommendation). Evidence: `node --test test/research-access.test.mjs` 11/11 with the backfill added to the entrypoint table; `node --test test/research-backfill-stop.test.mjs` 19/19 including `restriction persists the pause at observation before cleanup`, `restriction pause reaches real SQLite access state`, `stored non-jobs destinations are excluded before navigation` and `paused or missing access state stops the CLI before any CDP contact`; `test-cdp-lease.mjs` 30 sections including the new observation-site pause proof; `test-retry-policy.mjs`, `test-page-state.mjs`, `test-jh-search.mjs`, `test-jh-migrate.mjs`, `test-search-linkedin-jobs.mjs`, `test-extraction-and-filter.mjs` and `test-save-to-sqlite.mjs` pass; `npm test` 103/103 with zero skips; `npm run verify:release` passes all six gates with the profile check active; `git diff --check` clean. No LinkedIn request was made; the canonical workspace and installed skills were not touched.
