# SmartRecruiters Submit Coordinate Recovery

Coordinate clicks are a last resort when the submit control is visible but inaccessible to DOM/CDP automation.

Before clicking, verify required fields, upload state, user authorization, and that the visible control belongs to the intended application. Use the same capture and click coordinate system. After clicking, require a confirmation page or success message; otherwise record the result as unconfirmed.
